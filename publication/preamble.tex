% The publication layer is intentionally small. Pandoc owns semantic conversion;
% kernelbook.sty owns the complete print identity and can be developed directly
% against the inspectable .build/publication/book.tex artifact.
\usepackage{publication/kernelbook}
