---
id: ch14
slug: kernels-and-deep-learning
title: Deep Learning from a Kernel Point of View
part: XI · Learning the Representation
order: 54
tier: advanced
prerequisites:
  - multiple-kernel-learning
objectives:
  - >-
    Derive random-feature, NNGP, and neural-tangent kernels from a network's
    width and training regime.
  - >-
    Recognize when a finite network is close enough to its infinite-width kernel
    for the approximation to be useful.
  - >-
    Build convolutional, graph, and sequence kernel layers from extraction,
    kernel embedding, and pooling.
  - >-
    Connect RKHS norm control to deformation stability and generalization
    without confusing the three claims.
  - >-
    Diagnose correlation collapse, bandwidth failure, and the absence of genuine
    feature learning.
review_status: draft
reviewers:
  technical: null
  pedagogical: null
  specialist: null
provenance: provenance/ch14.yml
verification_date: null
bibliography:
  - neal1996
  - cho2009
  - rahimi2007
  - jacot2018
  - chizat2019
  - mairal2016
  - bietti2019
  - chen2020
  - broomhead1988
  - bengio2006local
  - glorot2010
  - montufar2014
  - lee2018nngp
  - matthews2018
  - arora2019cntk
  - boucheron2005
  - damianou2013deepgp
  - lodhi2002
  - scholkopf1998
  - shankar2020
  - mallat2012
  - bruna2013
  - sifre2013
  - bartlett2017
  - neyshabur2018
  - shalevshwartz2014book
  - schoenberg1942
  - zhang2016l1
  - zhang2017ccnn
  - williams2001
  - smola2000
  - fine2001
  - bo2011
  - bouvrie2009
  - montavon2011
  - anselmi2015
  - scholkopf1997
  - cohen2016
  - raj2016
  - shervashidze2011
  - lei2017
  - kriege2020survey
  - du2019gntk
  - xu2019gin
  - kipf2017
  - togninalli2019wwl
  - chen2019bio
  - chen2019rkn
  - morrow2017
  - hou2017
---
# Deep Learning from a Kernel Point of View

<p class="lead">Deep neural networks and kernel machines are usually told as rival stories: one learns a hierarchy of features from data, the other fixes a feature map and learns only a linear function on top. The boundary is more interesting. At initialization, a fixed-depth network with independent fan-in-scaled weights has a Gaussian-process limit under explicit activation and width assumptions. Under a different set of assumptions, gradient flow in NTK parameterization has a deterministic tangent-kernel limit over controlled training times. Convolutional kernel networks make a third connection without a width limit: they build an exact RKHS map from patch kernels and pooling, then approximate it with finite anchor subspaces that may be trained end to end. These are three regimes, not three names for the same phenomenon. This chapter derives each one, states where its guarantee stops, and uses finite-width rank loss, correlation collapse, and anchor conditioning to show what can fail before the asymptotic or RKHS description becomes useful.</p>

## What it means to do deep learning with kernels {#motivation}

Before any construction, it is worth naming the objects we will keep meeting, because each is a different bridge between the two worlds. The first is the family of *dot-product kernels*. On vectors these take the form

$$
K(\mathbf{x}, \mathbf{x}') = \kappa\!\left(\mathbf{x}^\top \mathbf{x}' \right)
\qquad\text{or, in homogeneous form,}\qquad
K(\mathbf{x}, \mathbf{x}') = \lVert \mathbf{x} \rVert \, \lVert \mathbf{x}' \rVert \, \kappa\!\left(\frac{\mathbf{x}^\top \mathbf{x}'}{\lVert \mathbf{x} \rVert \, \lVert \mathbf{x}' \rVert} \right).
$$

They depend on the inputs only through angles and norms, exactly the quantities a single artificial neuron sees, which is why they are the natural kernels to place at the junction with neural networks. The second object is the *hierarchical composition of feature maps*: a deep kernel is one whose feature map is itself a composition,

$$
K(\mathbf{x}, \mathbf{x}') = \langle \Phi(\mathbf{x}), \Phi(\mathbf{x}') \rangle
\qquad\text{with}\qquad
\Phi(\mathbf{x}) = \varphi_2(\varphi_1(\mathbf{x})),
$$

mirroring the layer-by-layer structure of a network. The third is the *neural tangent kernel*, which captures the asymptotic behavior of overparametrized deep networks trained by gradient descent. The fourth is the family of *convolutional kernel networks* (CKNs), which combine convolutional and hierarchical kernel constructions with end-to-end learning.

That last phrase deserves a definition, since it is the one that sounds paradoxical. Kernels are supposed to be fixed in advance, so what could it mean to *learn* a kernel representation end to end from labels? The answer, which we will make precise through the Nyström method, is that we learn a finite-dimensional linear subspace of the RKHS onto which we project the data, and we choose that subspace by backpropagation to minimize the training loss. Doing deep learning with kernels means learning *where in the RKHS to look*, not abandoning the RKHS.

## Deep kernel machines and dot-product kernels {#deep-kernel-machines}

The simplest way to obtain a deep kernel is to compose two feature maps, and the cleanest place to do it is on the sphere. If \(\varphi_1\) sends inputs to the unit sphere of some Hilbert space and \(K\) is a dot-product kernel there, then

$$
K(\mathbf{x}, \mathbf{x}') = \kappa_2\!\left(\langle \varphi_1(\mathbf{x}), \varphi_1(\mathbf{x}') \rangle \right) = \kappa_2\!\left(\kappa_1(\mathbf{x}^\top \mathbf{x}') \right)
$$

is again a valid kernel (Cho and Saul, 2009b). Iterating this composition builds arbitrarily deep kernels, each layer a dot-product kernel applied to the output of the previous one. For this to make sense we need to know which functions \(\kappa\) turn an inner product into a positive definite kernel, and a classical theorem answers exactly that.

::: {.theorem #thm-39-1}
[Theorem (Schoenberg, 1942)]{.box-title}

Let \(\mathcal{X} = \mathbb{S}\) be the unit sphere of a Hilbert space \(\mathcal{H}_0\), and consider \(K : \mathcal{X}^2 \to \mathbb{R}\) given by \(K(\mathbf{x}, \mathbf{y}) = \kappa(\langle \mathbf{x}, \mathbf{y} \rangle_{\mathcal{H}_0})\). Then \(K\) is positive definite for *every* \(\mathcal{H}_0\) if and only if \(\kappa\) is smooth and admits an expansion \(\kappa(u) = \sum_i a_i u^i\) with non-negative coefficients \(a_i \geq 0\).

**Assumptions.** All domains, quantifiers, regularity conditions, and parameter restrictions stated in the result are in force; no converse is implied.
**Proof status.** No separate proof is attached to this result; independent technical review must determine whether the surrounding derivation is sufficient.
:::

The condition is intuitive in light of earlier chapters: a Taylor series with non-negative coefficients is a non-negative combination of the powers \(u \mapsto u^i\), and each power \(u^i = (\mathbf{x}^\top \mathbf{y})^i\) is the kernel of a tensor-power feature map, hence positive definite; positive definiteness is preserved under non-negative sums and pointwise limits. Requiring it for all \(\mathcal{H}_0\), including infinite-dimensional ones, is what forces the coefficients to be non-negative rather than merely making a specific Gram matrix work.

It is worth pausing on why the sphere is the right stage for this construction. A dot-product kernel reads only the angle between two normalized inputs, so composing kernels means composing maps between angles: the first layer turns the raw similarity \(\mathbf{x}^\top \mathbf{x}'\) into a new one \(\kappa_1(\mathbf{x}^\top \mathbf{x}')\), the second reshapes that similarity again through \(\kappa_2\), and so on. Each layer bends the geometry of the sphere, pulling some pairs of directions closer in feature space and pushing others apart, exactly as a layer of a neural network reshapes its representations. Depth, in this picture, is repeated reshaping of angular similarity, and Schoenberg's condition is the guarantee that no reshaping in the chain can ever destroy positive definiteness. This is the first appearance of the chapter's throughline: a deep architecture is a map into an RKHS, and here we are simply stacking such maps.

### A catalog of dot-product kernels {#dot-product-catalog}

Writing \(\mathbf{z}, \mathbf{z}'\) for points on the sphere, the kernels that recur in deep-network analysis are the following.

  Kernel                                                      Expression \(\kappa(\langle \mathbf{z}, \mathbf{z}' \rangle)\)
  ----------------------------------------------------------- ------------------------------------------------
  linear                                                      \(\langle \mathbf{z}, \mathbf{z}' \rangle\)
  exponential                                                 \(e^{\alpha(\langle \mathbf{z}, \mathbf{z}' \rangle - 1)}\)
  inverse polynomial                                          \(\dfrac{1}{2 - \langle \mathbf{z}, \mathbf{z}' \rangle}\)
  polynomial of degree \(p\)   \((c + \langle \mathbf{z}, \mathbf{z}' \rangle)^p\)
  arc-cosine of degree 1                                      \(\dfrac{1}{\pi}\left(\sin\theta + (\pi - \theta)\cos\theta \right),\quad \theta = \arccos\langle \mathbf{z}, \mathbf{z}' \rangle\)
  Vovk's of degree 3                                         \(\dfrac{1}{3}\,\dfrac{1 - \langle \mathbf{z}, \mathbf{z}' \rangle^3}{1 - \langle \mathbf{z}, \mathbf{z}' \rangle} = \dfrac{1}{3}\left(1 + \langle \mathbf{z}, \mathbf{z}' \rangle + \langle \mathbf{z}, \mathbf{z}' \rangle^2 \right)\)

It is worth reading this table as a menu of angular reshapings rather than a list of formulas. Every entry is a univariate function \(\kappa\) of a single number, the cosine of the angle between the two normalized inputs, and each shapes that cosine differently: the polynomial kernels sharpen it by raising it to a power, so that only nearly aligned directions retain appreciable similarity; the exponential kernel decays smoothly as the angle opens; the arc-cosine kernels bend it into the particular curve a rectified neuron traces. Choosing a dot-product kernel is therefore choosing how aggressively the layer should contract angular similarity, and Schoenberg's condition is the single admissibility test every choice on the menu must pass. Each satisfies it. The exponential kernel is the bridge to the everyday Gaussian: if \(\lVert \mathbf{z} \rVert = \lVert \mathbf{z}' \rVert = 1\), then

$$
\kappa_{\exp}(\langle \mathbf{z}, \mathbf{z}' \rangle) = e^{\alpha(\langle \mathbf{z}, \mathbf{z}' \rangle - 1)} = e^{-\frac{\alpha}{2}\lVert \mathbf{z} - \mathbf{z}' \rVert^2},
$$

because \(\lVert \mathbf{z} - \mathbf{z}' \rVert^2 = 2 - 2\langle \mathbf{z}, \mathbf{z}' \rangle\) on the sphere. So the Gaussian kernel is a dot-product kernel in disguise once inputs are normalized, and the arc-cosine kernel, as we will now see, is precisely what a layer of rectified neurons computes.

## Random feature kernels: the infinite-width limit {#random-feature-kernels}

Consider a one-hidden-layer network with \(m\) neurons,

$$
f_\theta(\mathbf{x}) = \frac{1}{\sqrt{m}} \sum_{i=1}^m v_i \, \sigma(\mathbf{w}_i^\top \mathbf{x}),
$$

and ask what happens as the width \(m \to \infty\). There are two natural ways to let it grow, and each produces a kernel. In the first, due to Neal (1996) and revived by Rahimi and Recht (2007) under the name random features, we *freeze* the hidden weights at their random initialization \(\mathbf{w}_i \sim \mathcal{N}(0, I)\) and train only the output weights \(\theta = (v_i)_i\). The network is then linear in its parameters, a linear model over the random features \(\mathbf{x} \mapsto \sigma(\mathbf{w}_i^\top \mathbf{x})\), and by the law of large numbers the associated kernel converges to an expectation over the weight distribution.

:::: {.definition #def-39-2}
[Definition (random feature kernel)]{.box-title}

With hidden weights drawn as \(\mathbf{w} \sim \mathcal{N}(0, I)\), the random feature kernel is

$$
K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w} \sim \mathcal{N}(0, I)}\!\left[ \sigma(\mathbf{w}^\top \mathbf{x}) \, \sigma(\mathbf{w}^\top \mathbf{y}) \right].
$$
::::

There is a third, older way to read this limit that explains where the kernel comes from in the first place. Fix the inputs and let the random weights vary: the output \(f_\theta(\mathbf{x})\) of the untrained network is then a random variable, and as the width grows it is a sum of many independent bounded contributions, so by the central limit theorem it becomes Gaussian. A collection of outputs at several inputs becomes jointly Gaussian, which is to say the infinitely wide network at initialization is a Gaussian process, and its covariance function is exactly \(K_{\mathrm{RF}}\). This is Neal's (1996) observation, the historical root of the whole subject: a prior over network weights induces a prior over functions, and in the wide limit that prior is a Gaussian process with a kernel we can write down. Freezing the hidden layer and training only the output weights is then nothing but doing kernel regression with that kernel.

Two readings of the same object help here. Statistically, \(K_{\mathrm{RF}}\) is an expectation, and a width-\(m\) network computes an empirical average of \(m\) independent draws of the feature product \(\sigma(\mathbf{w}^\top \mathbf{x})\,\sigma(\mathbf{w}^\top \mathbf{y})\). By the law of large numbers that average converges to \(K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y})\) as \(m\) grows, with a fluctuation that shrinks like \(1/\sqrt{m}\). Architecturally, this says that a wide random hidden layer *is* a Monte Carlo estimate of a kernel: every neuron contributes one sample, and widening the layer refines the estimate. This is the precise sense in which an infinitely wide random network and a kernel machine are the same object, and it is worth watching the estimate settle.

The random-feature convergence plate in [[ch:large-scale-kernels|the large-scale-kernels chapter]] shows the same Monte Carlo mechanism for Fourier features. Read in the present setting, its feature count is network width: widening reduces sampling error around a fixed population kernel, but does not by itself make the representation label-adaptive.

This is the population inner product of the random features, and the finite network approximates it with \(m\) Monte Carlo samples. [[ch:large-scale-kernels]] met such integral representations for translation-invariant kernels, where the features were random Fourier modes. The point here is that they are available for many dot-product kernels too. Cho and Saul (2009b) give the family

$$
k_n(\mathbf{x}, \mathbf{y}) = \frac{1}{\pi} \lVert \mathbf{x} \rVert^n \, \lVert \mathbf{y} \rVert^n \, J_n(\theta),
\qquad
\theta = \cos^{-1}\!\frac{\mathbf{x}^\top \mathbf{y}}{\lVert \mathbf{x} \rVert \, \lVert \mathbf{y} \rVert},
$$

the arc-cosine kernels, where the angular part is

$$
J_n(\theta) = (-1)^n (\sin\theta)^{2n+1} \left(\frac{1}{\sin\theta} \frac{\partial}{\partial\theta} \right)^{\! n} \frac{\pi - \theta}{\sin\theta}.
$$

The first three are

$$
J_0(\theta) = \pi - \theta, \qquad
J_1(\theta) = \sin\theta + (\pi - \theta)\cos\theta, \qquad
J_2(\theta) = 3\sin\theta\cos\theta + (\pi - \theta)(1 + 2\cos^2\theta).
$$

Comparing \(J_1\) to the table above shows that \(k_1\) restricted to the sphere is exactly the arc-cosine kernel of degree 1. And these kernels are random feature kernels for a specific activation.

:::: {.theorem #thm-39-3}
[Theorem (Cho and Saul, 2009a)]{.box-title}

For the rectified power activation \(\sigma(u) = \dfrac{u^n}{\sqrt{2}}\left(1 + \operatorname{sign}(u) \right)\), that is \(\sigma(u) = \sqrt{2}\, u^n\) for \(u \gt 0\) and \(0\) otherwise, the arc-cosine kernel is a random feature kernel:

$$
k_n(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w} \sim \mathcal{N}(0, I)}\!\left[ \sigma(\mathbf{w}^\top \mathbf{x}) \, \sigma(\mathbf{w}^\top \mathbf{y}) \right].
$$

**Assumptions.** All domains, quantifiers, regularity conditions, and parameter restrictions stated in the result are in force; no converse is implied.
**Proof status.** No separate proof is attached to this result; independent technical review must determine whether the surrounding derivation is sufficient.
::::

The theorem is not merely asserted here; both low-order cases can be derived by hand from a two-dimensional Gaussian integral, and it is worth doing so once, because the same integral is the engine of the depth recursion below.

::::::: {.proof}
[Proof (arc-cosine \(k_0\) and \(k_1\))]{.box-title}

Both kernels are expectations over \(\mathbf{w} \sim \mathcal{N}(0, I)\) of a product of activations, and both depend on the inputs only through their norms and the angle \(\theta\), so fix the plane spanned by \(\mathbf{x}\) and \(\mathbf{y}\) and integrate the in-plane part of \(\mathbf{w}\) there. Write it in polar form, radius \(r\) with density \(r\,e^{-r^2/2}\) on \([0, \infty)\) and angle \(\varphi\) uniform on \([0, 2\pi)\), independent. Aligning \(\mathbf{x}\) with \(\varphi = 0\) and placing \(\mathbf{y}\) at angle \(\theta\),

$$ \mathbf{w}^\top \mathbf{x} = r\lVert\mathbf{x}\rVert\cos\varphi, \qquad \mathbf{w}^\top \mathbf{y} = r\lVert\mathbf{y}\rVert\cos(\varphi - \theta). $$

For degree zero, \(\sigma(u) = \sqrt{2}\,\Theta(u)\) with \(\Theta\) the Heaviside step, so the product \(\sigma(\mathbf{w}^\top\mathbf{x})\,\sigma(\mathbf{w}^\top\mathbf{y})\) equals \(2\) when both cosines are positive and \(0\) otherwise. Both are positive exactly for \(\varphi \in (\theta - \tfrac{\pi}{2}, \tfrac{\pi}{2})\), an arc of length \(\pi - \theta\), so with \(\varphi\) uniform

$$ k_0(\mathbf{x}, \mathbf{y}) = 2\,\mathbb{P}\!\left(\cos\varphi \gt 0,\ \cos(\varphi - \theta) \gt 0\right) = 2\cdot\frac{\pi - \theta}{2\pi} = \frac{\pi - \theta}{\pi} = \frac{J_0(\theta)}{\pi}. $$

For degree one, \(\sigma(u) = \sqrt{2}\,\max(0, u)\), and the radius and angle separate,

$$ k_1(\mathbf{x}, \mathbf{y}) = 2\,\mathbb{E}[r^2]\,\lVert\mathbf{x}\rVert\,\lVert\mathbf{y}\rVert \cdot \frac{1}{2\pi}\int_{\theta - \pi/2}^{\pi/2} \cos\varphi\,\cos(\varphi - \theta)\,d\varphi. $$

The radial moment is \(\mathbb{E}[r^2] = \int_0^\infty r^2\cdot r\,e^{-r^2/2}\,dr = 2\). Using \(\cos\varphi\cos(\varphi-\theta) = \tfrac{1}{2}[\cos\theta + \cos(2\varphi - \theta)]\), the angular integral over the arc of length \(\pi - \theta\) equals \(\tfrac{1}{2}\big[\sin\theta + (\pi - \theta)\cos\theta\big] = \tfrac{1}{2}J_1(\theta)\). Collecting the constants \(2\cdot 2\cdot\tfrac{1}{2\pi}\cdot\tfrac{1}{2} = \tfrac{1}{\pi}\),

$$ k_1(\mathbf{x}, \mathbf{y}) = \frac{\lVert\mathbf{x}\rVert\,\lVert\mathbf{y}\rVert}{\pi}\,J_1(\theta), $$

which on the unit sphere is \(k_1 = J_1(\theta)/\pi\). The same polar reduction with \(\sigma(u) = \sqrt{2}\,u^n\) for \(u \gt 0\) yields \(J_n\) for every \(n\). [\(\square\)]{.qed}
:::::::

The case \(n = 1\) is the one to remember, because there the rectified power is proportional to the rectified linear unit and

$$
k_1(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w} \sim \mathcal{N}(0, I)}\!\left[ \operatorname{ReLU}(\mathbf{w}^\top \mathbf{x}) \, \operatorname{ReLU}(\mathbf{w}^\top \mathbf{y}) \right].
$$

A layer of infinitely many random ReLU neurons computes, in expectation, the degree-1 arc-cosine kernel. This identity is one of the fundamental tools for analyzing ReLU networks, and it makes the arc-cosine kernel the canonical dot-product kernel of the deep-learning era.

There is a vivid geometric reading behind these formulas. Draw a random hyperplane through the origin with normal \(\mathbf{w} \sim \mathcal{N}(0, I)\). The degree-0 arc-cosine kernel measures the probability that \(\mathbf{x}\) and \(\mathbf{y}\) land on the same side of it, and this depends only on the angle \(\theta\) between them: the closer their directions, the fewer random hyperplanes fall between them, and indeed \(J_0(\theta) = \pi - \theta\) decreases linearly as the angle opens from \(0\) to \(\pi\). Raising the degree to \(n = 1\) keeps this same counting of sign agreements but weights each agreement by how strongly both points activate the neuron, which is exactly what a random ReLU unit does. The angle, not the raw coordinates, is the only thing these kernels ever see, matching the invariance a rectified neuron has to rotations of its weight vector and giving a concrete geometric meaning to what it means to embed data by a random layer.

### The deep limit: the NNGP covariance recursion {#nngp-recursion}

Neal's argument was for a single hidden layer, but the same central-limit mechanism applies at every layer of a deep network, and this is what turns one random-feature kernel into a recursion on depth [@lee2018nngp; @matthews2018]. The statement is a prior-at-initialization result, not a training result. Fix the depth and a finite collection of deterministic inputs. At every layer draw weights and biases independently across units and layers, with \(W_{ij}^{(\ell)} \sim \mathcal{N}(0,\sigma_w^2/n_{\ell-1})\) and \(b_i^{(\ell)} \sim \mathcal{N}(0,\sigma_b^2)\), and do not train them. The widths may tend to infinity sequentially or along the increasing width functions admitted by Theorem 4 of Matthews et al. (2018). For a continuous activation obeying their linear-envelope condition, \(\lvert\phi(u)\rvert \leq a+b\lvert u\rvert\), the finite-dimensional distributions converge to those of a Gaussian process and its covariance is propagated by the following rule.

:::: {.proposition #prop-39-4}
[Proposition (NNGP covariance recursion)]{.box-title}

Under the fixed-depth, independent Gaussian initialization and activation conditions above, the infinite-width pre-activation covariance obeys \(K^{(0)}(\mathbf{x}, \mathbf{y}) = \sigma_w^2\,\dfrac{\mathbf{x}^\top\mathbf{y}}{n_0} + \sigma_b^2\) and, for \(\ell \geq 1\),

$$ K^{(\ell)}(\mathbf{x}, \mathbf{y}) = \sigma_b^2 + \sigma_w^2\, \mathbb{E}_{(u,v)\sim\mathcal{N}(0,\, \Lambda^{(\ell-1)})}\!\left[\phi(u)\,\phi(v)\right], \qquad \Lambda^{(\ell-1)} = \begin{pmatrix} K^{(\ell-1)}(\mathbf{x},\mathbf{x}) & K^{(\ell-1)}(\mathbf{x},\mathbf{y}) \\ K^{(\ell-1)}(\mathbf{x},\mathbf{y}) & K^{(\ell-1)}(\mathbf{y},\mathbf{y}) \end{pmatrix}. $$

The map \(K^{(\ell-1)} \mapsto K^{(\ell)}\) depends on the previous kernel only through the three covariances in \(\Lambda^{(\ell-1)}\), and the expectation it applies is the *dual activation* of \(\phi\).

**Assumptions.** The input set used in the convergence claim is finite, or countable with the product topology used by Matthews et al.; depth is fixed; all weights and biases are independent with the displayed fan-in scaling; \(\sigma_w,\sigma_b\) are fixed; and \(\phi\) is continuous with a linear envelope. The conclusion is convergence in distribution of initialized random functions. It does not assert uniform convergence over an uncountable input domain, a rate in width, posterior convergence after conditioning, or agreement after training.

**Proof status.** The covariance identity follows from conditioning and the law of large numbers as sketched below. The Gaussian-process convergence, including the exchangeable-array central limit step that a naive layerwise iid argument misses, is localized to Condition 1, Lemma 2, and Theorem 4 of [@matthews2018].
::::

The proof has two distinct steps. First condition on layer \(\ell - 1\). A unit at layer \(\ell\) forms \(h_i^{(\ell)}(\mathbf{x}) = \sum_j W_{ij}\,\phi(h_j^{(\ell-1)}(\mathbf{x})) + b_i\), and direct covariance calculation gives \(\sigma_b^2\) plus \(\sigma_w^2\) times the empirical average of \(\phi(h_j(\mathbf{x}))\phi(h_j(\mathbf{y}))\). The law of large numbers sends that average to the displayed Gaussian expectation. Second, one must prove joint Gaussian convergence rather than merely identify a candidate covariance. Beyond the first hidden layer the summands are exchangeable through their shared random predecessor, not unconditionally iid. Matthews et al. establish the required moment bounds from the linear envelope and apply an exchangeable-array central limit theorem through finite linear projections. That second step is why the proposition states a genuine convergence theorem instead of treating covariance convergence as sufficient.

For the ReLU \(\phi = \max(0, \cdot)\), the dual-activation expectation is exactly the arc-cosine kernel \(k_1\) derived above, so the NNGP recursion is nothing but the arc-cosine composition of the opening section. On normalized inputs, where each layer is rescaled to keep \(K^{(\ell)}(\mathbf{x}, \mathbf{x}) = 1\), it collapses to a one-dimensional map on the correlation \(c = K(\mathbf{x}, \mathbf{y})\),

$$ c^{(\ell)} = \frac{1}{\pi}\Big(\sqrt{1 - (c^{(\ell-1)})^2} + (\pi - \arccos c^{(\ell-1)})\,c^{(\ell-1)}\Big) = \frac{J_1(\arccos c^{(\ell-1)})}{\pi}. $$

This is the precise sense in which a deep random ReLU network is a Gaussian process whose kernel is a fixed function of depth; the Gaussian-process viewpoint that makes such a prior over functions computable is developed in [[ch:gaussian-processes-and-rvm]]. The recursion is easy to run by hand.

:::: {.algorithm #algo-39-1}
[Algorithm (NNGP / arc-cosine kernel by depth)]{.box-title}

::: algo-io
[Input]{.algo-lab} unit inputs \(\mathbf{x}, \mathbf{y}\) on the sphere, depth \(L\), ReLU dual activation.

[Output]{.algo-lab} NNGP correlation \(c^{(L)}\) at depth \(L\).
:::

1.  Set \(c^{(0)} = \mathbf{x}^\top \mathbf{y}\).
2.  For \(\ell = 1, \ldots, L\): let \(\theta = \arccos c^{(\ell-1)}\) and set \(c^{(\ell)} = \tfrac{1}{\pi}\big(\sin\theta + (\pi - \theta)\cos\theta\big)\).
3.  Return \(c^{(L)}\). For a general activation replace the update by \(\mathbb{E}_{\mathcal{N}(0,\Lambda)}[\phi(u)\phi(v)]\) normalized by the running diagonal.
::::

:::: {.example #example-39-1}
[Example (arc-cosine kernel and its depth recursion)]{.box-title}

::: wex
Two unit vectors \(\mathbf{z} = (1, 0)\) and \(\mathbf{z}' = \big(\tfrac{1}{2}, \tfrac{\sqrt{3}}{2}\big)\), so \(\langle \mathbf{z}, \mathbf{z}'\rangle = 0.5\) and \(\theta_0 = \arccos 0.5 = \pi/3 = 60^\circ\); activation ReLU, weights \(\mathbf{w}\sim\mathcal{N}(0, I)\).

1.  [Evaluate the angular parts.]{.wex-op} At \(\theta_0 = 60^\circ\), \(J_0 = \pi - \theta_0 = 2.094395\) and \(J_1 = \sin\theta_0 + (\pi - \theta_0)\cos\theta_0 = 0.866025 + 2.094395\cdot 0.5 = 1.913223\).
2.  [Read off the two kernels.]{.wex-op} \(k_0 = J_0/\pi = 0.666667\) and \(k_1 = J_1/\pi = 0.608998\). The half-space overlap probability is \(\mathbb{P}(\text{both} \gt 0) = (\pi - \theta_0)/(2\pi) = 0.333333\), and indeed \(k_0 = 2\cdot 0.333333\).
3.  [Cross-check against random features.]{.wex-op} With \(4{,}000{,}000\) draws \(\mathbf{w}\sim\mathcal{N}(0, I)\): \(\mathbb{E}[\operatorname{ReLU}\cdot\operatorname{ReLU}] \approx 0.3046\) against the closed \(J_1/(2\pi) = 0.3045\), so \(2\times\) that \(\approx 0.6093\) matches \(k_1 = 0.609\); and the empirical \(\mathbb{P}(\text{both}\gt 0) \approx 0.3335\) matches \(0.3333\).
4.  [Propagate through depth.]{.wex-op} Apply \(c \mapsto J_1(\arccos c)/\pi\): \(c^{(0)} = 0.5 \to c^{(1)} = 0.608998 \to c^{(2)} = 0.683906 \to c^{(3)} = 0.738128\). The correlation climbs monotonically with depth.
5.  [Locate the fixed point.]{.wex-op} Iterating the map to convergence gives \(c^\star \approx 0.999034\), so depth drives every pair of distinct directions toward perfect correlation.

**Reading.** One layer of infinitely many random ReLUs turns \(\cos 60^\circ = 0.5\) into \(0.609\), and the Monte Carlo layer of \(4\) million neurons reproduces the closed form to three decimals, the finite-width estimate of the exact kernel. Stacking layers pushes correlations toward \(1\): this is the degeneracy that makes very deep ReLU NNGP kernels lose discriminative power unless the scale is managed carefully.
:::

**Verification artifact.** checks/example-ch14-example-39-1.json records the example source hash and verification scope.
::::

The limit is useful only if the finite network is close on the scale the task cares about. Two errors matter: Monte Carlo error in the empirical kernel and movement of the learned features away from initialization. Both often shrink with width in a lazy scaling, but their constants and the relevant tolerance are problem dependent.

<figure class="viz" data-figure="finite-vs-infinite-width" data-alt="A log-log plot shows kernel sampling error and feature movement decreasing with network width, crossing a chosen approximation tolerance only at sufficiently large width.">
<figcaption>A network is legitimately described by its infinite-width kernel only after both empirical-kernel error and feature movement fall below a task-level tolerance. Width alone is not the certificate.</figcaption>
</figure>

:::: {.example #example-39-3}
[Example (a finite-width kernel can be rank deficient)]{.box-title}

::: wex
Draw \(16\) deterministic unit inputs in \(\mathbb{R}^{12}\) with seed \(1401\). Their analytic normalized ReLU kernel is

$$
K_{ij}=\frac{\sqrt{1-c_{ij}^2}+(\pi-\arccos c_{ij})c_{ij}}{\pi},
\qquad c_{ij}=\mathbf{x}_i^\top\mathbf{x}_j.
$$

For each width \(m\), draw a fresh Gaussian matrix \(W\) with seed \(1401+m\), form \(F=\sqrt{2/m}\operatorname{ReLU}(XW^\top)\), and compare \(\widehat K_m=FF^\top\) with \(K\).

  width \(m\)   relative Frobenius error   numerical rank of \(\widehat K_m\)
  -----------  --------------------------  -----------------------------------
  8            0.512743                    8
  32           0.419790                    16
  128          0.188895                    16
  512          0.116120                    16
  2048         0.051238                    16

The limiting \(K\) has rank \(16\) and smallest eigenvalue \(0.093207\). At width \(8\), however, \(\widehat K_8\) has rank at most \(8\) by construction and its relative error exceeds one half. The limit is correct while the finite model fails to preserve even the rank needed to interpolate arbitrary labels on these inputs. The error generally shrinks with width but need not do so monotonically for a single sequence of random draws. A finite-width claim therefore needs a probability level, a target norm, and a task tolerance, not only the phrase “sufficiently wide.”
:::

**Verification artifact.** checks/example-ch14-example-39-3.json records the example source hash and verification scope.
::::

## Neural tangent kernels: the trajectory of gradient descent {#neural-tangent-kernels}

Freezing the hidden layer is a strong assumption, and the more interesting question is what happens when we train *all* the parameters \(\theta = (v_i, \mathbf{w}_i)_i\) by gradient descent. The answer depends on parameterization and training scale. In the NTK parameterization, each fan-in sum carries an inverse-square-root width factor, depth stays fixed, the learning-rate or gradient-flow time scale stays \(O(1)\), and initialization is iid Gaussian. Under the smooth-activation and bounded-trajectory hypotheses of Theorems 1 and 2 of [@jacot2018], sequential infinite-width limits make the tangent kernel deterministic and constant over finite training time. The network is then well approximated by its first-order Taylor expansion around initialization,

$$
f_\theta(\mathbf{x}) \approx f_{\theta_0}(\mathbf{x}) + \big\langle \theta - \theta_0, \, \nabla_\theta f_\theta(\mathbf{x})\big|_{\theta = \theta_0} \big\rangle.
$$

This linearized model is linear in the deviation \(\theta - \theta_0\), with feature map the gradient \(\nabla_\theta f_{\theta_0}(\mathbf{x})\). Training it by gradient descent is therefore linear regression in that fixed feature space, and its kernel is the inner product of the gradients.

The \(1/\sqrt{m}\) factor explains how an order-one function change can coexist with small movement per neuron, but it is not by itself a proof of laziness. One must also control the training path so that the Jacobian remains close to its initial value, keep the time horizon or integrated training direction bounded, and use a limiting tangent Gram matrix that is nondegenerate on the training inputs when convergence to interpolation is claimed. A different output scaling, a width-dependent learning rate, long training beyond the controlled horizon, a singular tangent Gram matrix, or mean-field scaling can produce another regime. Chizat, Oyallon, and Bach call the controlled linearized case *lazy training* [@chizat2019]; the name labels a regime to verify, not a consequence granted to every wide network.

:::: {.definition #def-39-5}
[Definition (neural tangent kernel)]{.box-title}

$$
K_{\mathrm{NTK}}(\mathbf{x}, \mathbf{y}) = \lim_{m \to \infty} \big\langle \nabla_\theta f_{\theta_0}(\mathbf{x}), \, \nabla_\theta f_{\theta_0}(\mathbf{y}) \big\rangle.
$$
::::

For the original theorem, the activation is Lipschitz, twice differentiable, and has bounded second derivative. ReLU does not satisfy that smoothness assumption at the origin. Its initialization recursion below remains meaningful with the almost-everywhere derivative, but transferring the full training-dynamics conclusion to finite ReLU networks requires a separate nonsmooth theorem and its own width, step-size, and data assumptions.

::: {.proposition #prop-39-6}
[Proposition (NTK gradient flow; Jacot, Gabriel, and Hongler 2018)]{.box-title}

Fix a finite training set, a fully connected network of fixed depth in NTK parameterization, iid Gaussian initialization, and an activation that is Lipschitz and twice differentiable with bounded second derivative. Take hidden widths to infinity sequentially and consider gradient flow on squared loss over a finite time interval for which the integrated training direction is bounded. Then the tangent Gram matrix converges to a deterministic limit \(\Theta\), remains constant in the limit, and the residual vector \(\mathbf{r}_t=\mathbf{f}_t-\mathbf{y}\) obeys

$$
\dot{\mathbf r}_t=-\Theta\mathbf r_t,
\qquad
\mathbf r_t=e^{-t\Theta}\mathbf r_0.
$$

If, in addition, \(\lambda_{\min}(\Theta)\gt 0\) on the training inputs, then \(\mathbf r_t\to 0\). The limiting predictor retains the initial-function term; zero initialization or explicit centering is needed before identifying it with the usual minimum-norm kernel interpolant. Finite-time stopping applies the spectral filter \(1-e^{-t\lambda}\). A ridge solution requires a separately specified regularized objective and parameterization; ordinary parameter decay is not automatically identical to RKHS ridge regression.

**Assumptions.** The quantifiers and regime are exactly those in the preceding paragraph. Positive definiteness is needed only for the interpolation conclusion, not for writing the flow.

**Proof status.** The function-space identity \(\partial_t f_t=-K_{\theta_t}\nabla_f C\) follows by the chain rule. Constancy of \(K_{\theta_t}\) in the sequential infinite-width limit is Theorem 2 of [@jacot2018], building on the initialization limit in its Theorem 1. Diagonalizing the fixed matrix \(\Theta\) proves the displayed exponential solution and the early-stopping filter.
:::

For our two-layer network the gradient has two blocks, one from the output weights \(v_i\) and one from the hidden weights \(\mathbf{w}_i\), and computing the inner product of each in the limit gives

$$
K_{\mathrm{NTK}}(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w}}\!\left[ \sigma(\mathbf{w}^\top \mathbf{x}) \, \sigma(\mathbf{w}^\top \mathbf{y}) + (\mathbf{x}^\top \mathbf{y}) \, \sigma'(\mathbf{w}^\top \mathbf{x}) \, \sigma'(\mathbf{w}^\top \mathbf{y}) \right].
$$

The first term is exactly the random feature kernel from differentiating with respect to the output weights; the second, from differentiating with respect to the hidden weights, brings in the derivative \(\sigma'\) and an explicit \(\mathbf{x}^\top \mathbf{y}\) factor. With ReLU networks \(\sigma'\) is a step function, both expectations are functions of the angle alone, and \(K_{\mathrm{NTK}}\) is again a dot-product kernel, assembled from the arc-cosine kernels of the previous section.

The decomposition also sharpens the difference between the two limits. Freezing the hidden layer keeps only the first term, the random feature kernel, because the hidden weights contribute no gradient; training all the parameters adds the second term, which is precisely the extra similarity carried by the hidden weights being allowed to move a little. That the two limits give different kernels is the whole point: the random feature kernel describes a network that never adapts its features, while the neural tangent kernel describes one whose features do move, but only infinitesimally, so that the movement is still captured by a fixed linear model. Neither limit performs genuine feature learning, in which the representation would reorganize itself around the data; that reorganization is the subject of [[ch:the-frontier]]. That is what makes both objects tractable and, at the same time, what marks the boundary of what these kernels can explain about finite networks, which do learn their features. The lesson to carry forward is that the very same wide network yields a kernel whether we freeze it or train it; only which kernel changes.

### Depth: the neural tangent kernel recursion {#ntk-recursion}

The two-term formula was written for a single hidden layer, but for a deep network the tangent kernel obeys its own recursion, assembled from the same dual activations that drove the NNGP recursion (Jacot et al., 2018; Arora et al., 2019). Alongside the NNGP kernel \(\Sigma^{(\ell)}\), which is the dual activation of \(\sigma\), one needs the *derivative kernel* \(\dot\Sigma^{(\ell)}\), the dual activation of \(\sigma'\), because backpropagating a gradient through a layer multiplies by \(\sigma'\).

::::: {.proposition #prop-39-7}
[Proposition (deep NTK recursion)]{.box-title}

For a fixed-depth fully connected network in NTK parameterization, with the iid Gaussian initialization and smooth-activation conditions stated above, let \(\Sigma^{(\ell)}\) be the NNGP kernels of the previous recursion and define the derivative kernels

$$ \dot\Sigma^{(\ell)}(\mathbf{x},\mathbf{y}) = \mathbb{E}_{(u,v)\sim\mathcal{N}(0,\, \Lambda^{(\ell-1)})}\!\left[\sigma'(u)\,\sigma'(v)\right], \qquad \Lambda^{(\ell-1)} \text{ built from } \Sigma^{(\ell-1)}. $$

Then the neural tangent kernel is assembled by

$$ \Theta^{(0)} = \Sigma^{(0)}, \qquad \Theta^{(\ell)}(\mathbf{x},\mathbf{y}) = \Sigma^{(\ell)}(\mathbf{x},\mathbf{y}) + \Theta^{(\ell-1)}(\mathbf{x},\mathbf{y})\,\dot\Sigma^{(\ell)}(\mathbf{x},\mathbf{y}). $$

For ReLU the corresponding initialization recursion, interpreted using its almost-everywhere derivative, has \(\Sigma^{(\ell)}\) proportional to \(k_1\) and \(\dot\Sigma^{(\ell)}\) proportional to \(k_0\). This algebraic recursion does not remove the nonsmooth-training caveat above.

**Assumptions.** Width limits are sequential, depth and the input set are fixed, all layers use the same stated parameterization, and the activation and derivative moments exist. Constancy during training additionally requires the hypotheses of the NTK gradient-flow proposition.

**Proof status.** The layerwise gradient decomposition below proves the recursion once the NNGP and derivative-kernel limits are available. The limiting statement and parameterization are localized to Theorem 1 and Section 4 of [@jacot2018].
:::::

Each layer contributes one block to the parameter gradient. Differentiating the output with respect to the weights of layer \(\ell\) produces the product of the representation feeding into that layer, whose inner product is the NNGP kernel \(\Sigma^{(\ell-1)}\), with the Jacobian backpropagated from \(\ell\) to the top, whose inner product is a running product of derivative kernels. Summing the per-layer contributions and grouping them telescopes into the recursion above: each deeper layer multiplies the accumulated tangent kernel by the local derivative kernel \(\dot\Sigma^{(\ell)}\) and adds the fresh NNGP term \(\Sigma^{(\ell)}\). The two-term formula for one hidden layer is exactly the case \(L = 1\), whose summands \(\Sigma^{(1)}\) and \(\Theta^{(0)}\dot\Sigma^{(1)} = (\mathbf{x}^\top\mathbf{y})\,\mathbb{E}[\sigma'\sigma']\) are the two terms above. Because \(\sigma' = \Theta\) for ReLU, the derivative kernel is the degree-0 arc-cosine kernel and the NNGP kernel is the degree-1, so the whole deep NTK is built from the closed forms of the opening section; its spectral behavior, which governs what such a kernel can learn, is the concern of [[ch:mercer-and-rates]], and its finite approximation that of [[ch:large-scale-kernels]].

:::: {.example #example-39-2}
[Example (deep ReLU NTK by recursion)]{.box-title}

::: wex
The same two unit vectors, \(c^{(0)} = 0.5\), \(\theta_0 = 60^\circ\). He normalization (a factor \(2\) in each dual activation) keeps the diagonal at \(1\), so on the sphere \(\Sigma^{(\ell)} = J_1(\theta_{\ell-1})/\pi\) and \(\dot\Sigma^{(\ell)} = (\pi - \theta_{\ell-1})/\pi\) with \(\theta_{\ell-1} = \arccos c^{(\ell-1)}\). Depth \(L = 3\).

1.  [Layer 1.]{.wex-op} From \(c^{(0)} = 0.5\): \(\Sigma^{(1)} = 0.608998\), \(\dot\Sigma^{(1)} = 0.666667\), so \(\Theta^{(1)} = \Sigma^{(1)} + \Theta^{(0)}\dot\Sigma^{(1)} = 0.608998 + 0.5\cdot 0.666667 = 0.942331\).
2.  [Layer 2.]{.wex-op} Now \(c^{(1)} = 0.608998\): \(\Sigma^{(2)} = 0.683906\), \(\dot\Sigma^{(2)} = 0.708428\), so \(\Theta^{(2)} = 0.683906 + 0.942331\cdot 0.708428 = 1.351480\).
3.  [Layer 3.]{.wex-op} Now \(c^{(2)} = 0.683906\): \(\Sigma^{(3)} = 0.738128\), \(\dot\Sigma^{(3)} = 0.739720\), so \(\Theta^{(3)} = 0.738128 + 1.351480\cdot 0.739720 = 1.737845\).
4.  [Normalize.]{.wex-op} On the diagonal every dual activation is \(1\), so \(\Theta^{(L)}(\mathbf{x},\mathbf{x}) = L + 1 = 4\); the NTK correlation is \(1.737845 / 4 = 0.434461\).

**Reading.** The tangent kernel is assembled from the same arc-cosine pieces as the NNGP kernel, but the accumulation \(\Theta^{(\ell)} = \Sigma^{(\ell)} + \Theta^{(\ell-1)}\dot\Sigma^{(\ell)}\) makes it grow with depth. After three layers two inputs at \(60^\circ\) have NTK correlation \(0.434\), well below the NNGP correlation \(0.738\) at the same depth, because the tangent kernel keeps a memory of every layer's contribution rather than only the last one.
:::

**Verification artifact.** checks/example-ch14-example-39-2.json records the example source hash and verification scope.
::::

## The Nyström bridge and end-to-end learning {#nystrom-and-end-to-end}

The kernels above are infinite-dimensional, so to compute with them we need a finite approximation, and the approximation itself turns out to look like a neural-network layer. This is the technical heart of the chapter, so we develop it carefully. Given a kernel \(K\) with feature map \(\varphi\) into an RKHS \(\mathcal{H}\), the Nyström method replaces any point \(\varphi(\mathbf{x})\) by its orthogonal projection onto a finite-dimensional subspace spanned by the embeddings of \(p\) *anchor points* \(Z = [\mathbf{z}_1, \ldots, \mathbf{z}_p]\),

$$
\mathcal{F} = \operatorname{span}\!\left(\varphi(\mathbf{z}_1), \ldots, \varphi(\mathbf{z}_p) \right).
$$

The projection is the best approximation of \(\varphi(\mathbf{x})\) inside \(\mathcal{F}\),

$$
\Pi_{\mathcal{F}}[\mathbf{x}] = \sum_{j=1}^p \beta_j^\star \, \varphi(\mathbf{z}_j),
\qquad
\beta^\star \in \arg\min_{\beta \in \mathbb{R}^p} \Big\lVert \varphi(\mathbf{x}) - \sum_{j=1}^p \beta_j \, \varphi(\mathbf{z}_j) \Big\rVert_{\mathcal{H}}^2,
$$

a least-squares problem solved by the normal equations. Writing everything through the kernel and, for a dot-product kernel \(K(\mathbf{x}, \mathbf{y}) = \kappa(\langle \mathbf{x}, \mathbf{y} \rangle)\), approximating the kernel by the inner product of projections gives a finite-dimensional embedding \(\psi\),

$$
K(\mathbf{x}, \mathbf{y}) \approx \langle \Pi_{\mathcal{F}}[\mathbf{x}], \Pi_{\mathcal{F}}[\mathbf{y}] \rangle_{\mathcal{H}} = \langle \psi(\mathbf{x}), \psi(\mathbf{y}) \rangle_{\mathbb{R}^p},
\qquad
\psi(\mathbf{x}) = \kappa(Z^\top Z)^{-1/2} \, \kappa(Z^\top \mathbf{x}),
$$

where \(\kappa\) is applied entrywise. Read the right-hand side left to right: \(Z^\top \mathbf{x}\) is a linear map, \(\kappa(\cdot)\) is a pointwise nonlinearity, and multiplication by \(\kappa(Z^\top Z)^{-1/2}\) is a second linear map. The Nyström embedding of a dot-product kernel *is* a small neural network layer: linear, nonlinearity, linear (Williams and Seeger, 2001; Smola and Schölkopf, 2000; Fine and Scheinberg, 2001).

This is what lets us learn kernels end to end. The encoding \(\psi_Z(\mathbf{x}) = \kappa(Z^\top Z)^{-1/2} \kappa(Z^\top \mathbf{x})\) is parametrized by the anchors \(Z\), and there are two ways to choose them. Unsupervised: run K-means on the data and take the centroids as anchors, so that \(\mathcal{F}\) covers the region of the RKHS where data actually live. Supervised: treat \(Z\) as network parameters and optimize them by backpropagation jointly with a linear predictor,

$$
\min_{\mathbf{w}, Z} \; \frac{1}{n} \sum_{i=1}^n L\!\left(y_i, \mathbf{w}^\top \psi_Z(\mathbf{x}_i) \right) + \lambda \lVert \mathbf{w} \rVert^2.
$$

End-to-end learning with kernels, then, means learning a parametrized linear subspace \(\mathcal{F}\) of the RKHS onto which the data are projected. The kernel supplies the geometry; learning chooses the subspace.

## Convolutional kernel networks {#ckn-overview}

We now scale this idea up from a single layer to a whole convolutional architecture. Three regimes must remain separate.

  Regime                         Fixed object                                      Learned object                         Valid conclusion
  ----------------------------- ------------------------------------------------- -------------------------------------- ---------------------------------------------------------
  exact convolutional kernel    patch kernels, extraction, and pooling operators  final RKHS linear functional           \(K_{\mathrm{CKN}}=\langle\Phi,\Phi'\rangle\) is PSD
  finite Nyström CKN            anchors and their regularized Gram factors         final finite-dimensional predictor     a projection approximation to the exact feature map
  supervised CKN                patch-kernel family and architecture               anchors and predictor, jointly         task-adapted finite subspaces, not one fixed kernel during training

The exact object is defined first. At layer \(k\), let patch extraction \(P_k\) be bounded between the stated \(L^2\) spaces, let \(M_k\) be the feature map of a normalized homogeneous dot-product kernel whose profile has a nonnegative power series, and let pooling \(A_k\) be bounded and linear. Their composition defines a deterministic feature map \(\Phi=A_nM_nP_n\cdots A_1M_1P_1\), hence a PSD kernel \(K_{\mathrm{CKN}}(\mathbf{x},\mathbf{x}')=\langle\Phi(\mathbf{x}),\Phi(\mathbf{x}')\rangle\). No width limit, random initialization, or gradient-flow claim enters this construction.

Mairal's practical construction replaces each exact \(M_k\) by finite Nyström coordinates [@mairal2016, Sections 2.2--2.3]. For fixed anchors this is a projection onto their RKHS span when the anchor Gram matrix is nonsingular; with a singular or ill-conditioned Gram matrix one must use a pseudoinverse or a declared ridge. Supervised training changes the anchors, so the finite representation and its induced approximate kernel change during optimization. What remains fixed is the chosen patch-kernel family and operator architecture, not the realized feature map.

:::: {.proposition #prop-39-8}
[Proposition (exact CKN validity and finite-subspace approximation)]{.box-title}

Suppose every local kernel \(K_k\) is PSD with feature map \(\varphi_k\), every \(P_k\) and \(A_k\) is a bounded linear operator on the displayed Hilbert-valued \(L^2\) spaces, and the composed map \(\Phi_n(\mathbf{x})\) has finite \(L^2\) norm. Then

$$
K_{\mathrm{CKN}}(\mathbf{x},\mathbf{x}')
=\langle\Phi_n(\mathbf{x}),\Phi_n(\mathbf{x}')\rangle_{L^2(\Omega,\mathcal H_n)}
$$

is PSD. If fixed anchors \(Z_k\) have invertible Gram matrices, replacing \(\varphi_k\) by the Nyström coordinates \(K_k(Z_k,Z_k)^{-1/2}K_k(Z_k,\cdot)\) gives the orthogonal projection of each local feature onto \(\operatorname{span}\{\varphi_k(z):z\in Z_k\}\). It equals the exact layer only on that span; otherwise it defines an approximate CKN whose error propagates through later bounded operators.

**Proof.** For coefficients \(a_1,\ldots,a_N\),

$$
\sum_{i,j=1}^N a_i a_j K_{\mathrm{CKN}}(\mathbf{x}_i,\mathbf{x}_j)
=\left\lVert\sum_{i=1}^N a_i\Phi_n(\mathbf{x}_i)\right\rVert^2\geq 0.
$$

For the second claim, write the orthogonal projection in the nonorthogonal anchor basis. Its coordinate Gram correction is \(K_k(Z_k,Z_k)^{-1/2}\), which yields the stated finite inner product. Equality holds when the local feature lies in the anchor span. Applying a later bounded operator \(B\) changes a local residual \(r\) by at most \(\lVert B\rVert\lVert r\rVert\); iterating gives the propagation statement. \(\square\)

**Scope.** PSD validity is exact. Approximation accuracy requires anchor coverage, conditioning, and operator-norm control. Optimizing anchors is a nonconvex representation-learning problem and is not covered by the fixed-kernel representer theorem.
::::

Two features of the exact decomposition make it worth the effort. First, before anchors are learned, \(\Phi\) depends only on the patch kernels and architecture, so it can be analyzed once and for all: we can ask whether it is stable to perturbations and whether it loses information about the signal. Second, a fixed final linear functional \(f\) is controlled by its RKHS norm through the defining inequality

$$
\lvert f(\mathbf{x}) - f(\mathbf{x}') \rvert \leq \lVert f \rVert_{\mathcal{H}} \, \lVert \Phi(\mathbf{x}) - \Phi(\mathbf{x}') \rVert_{\mathcal{H}}.
$$

The right-hand side factorizes cleanly. The geometry factor \(\lVert \Phi(\mathbf{x}) - \Phi(\mathbf{x}') \rVert_{\mathcal{H}}\) is a property of the architecture alone; the complexity factor \(\lVert f \rVert_{\mathcal{H}}\) is a property of the learned model. Controlling each separately is the program of the rest of the chapter.

This is the point at which the chapter's throughline stops being a slogan and becomes a construction. In the NNGP and neural-tangent stories the kernel appeared as a limit. Here the roles are reversed: we build an exact RKHS map first and approximate it by learned finite subspaces. An ordinary CNN is not thereby proved to belong to this RKHS. Containment results require a matching class of smooth homogeneous activations and finite norm, as developed by Bietti and Mairal; a ReLU CNN, an arbitrary normalization layer, or a trained finite CKN needs its own argument. The exact \(\Phi\) supports architecture-level stability analysis, whereas a supervised finite CKN must additionally report anchor conditioning and approximation error.

## Constructing the RKHS for continuous signals {#rkhs-continuous-signals}

To analyze stability to deformations honestly we must work with continuous signals, where a deformation is a smooth warp of the domain rather than a permutation of pixels. So we model an image as a map, not an array. The input is a continuous signal

$$
\mathbf{x}_0 : \Omega \to \mathcal{H}_0, \qquad \mathbf{x}_0 \in L^2(\Omega, \mathcal{H}_0),
$$

where \(\Omega = \mathbb{R}^d\) is the domain (\(d = 2\) for images) and \(\mathbf{x}_0(u) \in \mathcal{H}_0\) is the value at location \(u\) (with \(\mathcal{H}_0 = \mathbb{R}^3\) for RGB). Each layer transforms a feature map \(\mathbf{x}_{k-1} \in L^2(\Omega, \mathcal{H}_{k-1})\) into the next one \(\mathbf{x}_k \in L^2(\Omega, \mathcal{H}_k)\) by composing three operators,

$$
\mathbf{x}_k = A_k M_k P_k \, \mathbf{x}_{k-1}.
$$

Each operator has one job, and it is the interplay of the three that makes the theory work.

### Patch extraction \(P_k\) {#patch-extraction}

The first operator gathers local context. For a patch shape \(S_k \subset \Omega\) (a small box, say), \(P_k\) stacks the values of the feature map over a neighborhood of each point,

$$
P_k \mathbf{x}_{k-1}(u) := \left(\mathbf{x}_{k-1}(u + v) \right)_{v \in S_k} \; \in \; \mathcal{P}_k = \mathcal{H}_{k-1}^{S_k}.
$$

So \(P_k \mathbf{x}_{k-1}(u)\) is the patch of \(\mathbf{x}_{k-1}\) centered at \(u\), living in a product Hilbert space. This is a linear operator, and it is what will fail to commute with deformations at high frequencies, the technical obstacle we confront later.

### Nonlinear mapping \(M_k\) {#nonlinear-mapping}

The second operator embeds each patch through the kernel. Pointwise in \(u\),

$$
M_k P_k \mathbf{x}_{k-1}(u) := \varphi_k\!\left(P_k \mathbf{x}_{k-1}(u) \right) \; \in \; \mathcal{H}_k,
$$

where \(\varphi_k\) is the feature map of a homogeneous dot-product kernel on patches,

$$
K_k(\mathbf{z}, \mathbf{z}') = \lVert \mathbf{z} \rVert \, \lVert \mathbf{z}' \rVert \, \kappa_k\!\left(\frac{\langle \mathbf{z}, \mathbf{z}' \rangle}{\lVert \mathbf{z} \rVert \, \lVert \mathbf{z}' \rVert} \right) = \langle \varphi_k(\mathbf{z}), \varphi_k(\mathbf{z}') \rangle,
\qquad
\kappa_k(u) = \sum_{j=0}^\infty b_j u^j, \quad b_j \geq 0, \quad \kappa_k(1) = 1.
$$

The non-negative coefficients guarantee positive definiteness (Schoenberg), and \(\kappa_k(1) = 1\) normalizes the map so that \(\lVert \varphi_k(\mathbf{z}) \rVert = \lVert \mathbf{z} \rVert\). Standard choices are the Gaussian kernel on the sphere \(\kappa_{\exp}(\langle \mathbf{z}, \mathbf{z}' \rangle) = e^{\langle \mathbf{z}, \mathbf{z}' \rangle - 1}\) and the inverse polynomial \(\kappa_{\text{inv-poly}}(\langle \mathbf{z}, \mathbf{z}' \rangle) = 1/(2 - \langle \mathbf{z}, \mathbf{z}' \rangle)\). Being pointwise, \(M_k\) will commute with any warp of the domain, which is why it never causes trouble in the stability analysis.

### Pooling \(A_k\) {#pooling}

The third operator blurs, providing local invariance. It is a linear convolution against a Gaussian pooling filter at scale \(\sigma_k\),

$$
\mathbf{x}_k(u) = A_k M_k P_k \mathbf{x}_{k-1}(u) = \int_{\mathbb{R}^d} h_{\sigma_k}(u - v) \, M_k P_k \mathbf{x}_{k-1}(v) \, dv \; \in \; \mathcal{H}_k,
$$

with \(h_{\sigma_k}(u) = \sigma_k^{-d} h(u / \sigma_k)\) and \(h\) a Gaussian. Two properties matter. First, \(A_k\) is linear and non-expansive, \(\lVert A_k \rVert \leq 1\), so it can never amplify a perturbation. Second, in practice pooling is followed by *discretization*: we subsample at resolution \(\sigma_k\), which is what lets the feature maps shrink from layer to layer. The blur before the subsample is anti-aliasing, and information is preserved as long as the subsampling factor does not exceed the patch size, a claim we make precise in the discretization section.

### The multilayer representation and the CKN kernel {#multilayer-representation}

Composing \(n\) layers gives the feature map of the whole network,

$$
\Phi_n(\mathbf{x}) = A_n M_n P_n A_{n-1} M_{n-1} P_{n-1} \cdots A_1 M_1 P_1 \, \mathbf{x}_0 \; \in \; L^2(\Omega, \mathcal{H}_n).
$$

In practice the patch sizes \(S_k\) and pooling scales \(\sigma_k\) grow exponentially with depth, because each layer is subsampled and so a fixed patch covers an exponentially larger region of the original image. A prediction is a linear functional of this representation, \(f(\mathbf{x}) = \langle \mathbf{w}, \Phi_n(\mathbf{x}) \rangle\), and the corresponding kernel is the \"linear kernel\" on top of the feature maps,

$$
K_{\mathrm{CKN}}(\mathbf{x}, \mathbf{x}') = \langle \Phi_n(\mathbf{x}), \Phi_n(\mathbf{x}') \rangle_{L^2(\Omega)} = \int_\Omega \langle \mathbf{x}_n(u), \mathbf{x}'_n(u) \rangle \, du.
$$

Finally, real inputs are discrete signals acquired by a physical device, which we model as \(\mathbf{x}_0 = A_0 \mathbf{x}\), where \(\mathbf{x}\) is the underlying continuous signal and \(A_0\) is a local integrator at scale \(\sigma_0\) performing anti-aliasing. The full representation is then \(\Phi_n(A_0 \mathbf{x})\), and this is the object whose stability we study.

## Convolutional kernel networks in practice {#ckn-vs-cnn}

It helps to see exactly where a CKN and a CNN part ways, because they differ in one operator only. Given a patch \(\mathbf{x}\), a convolutional network computes

$$
\psi_{\mathrm{CNN}}(\mathbf{x}) = \sigma(Z^\top \mathbf{x}),
$$

a linear map followed by a pointwise nonlinearity, with \(Z\) the filters. A convolutional kernel network computes the Nyström embedding of the patch kernel,

$$
\psi_{\mathrm{CKN}}(\mathbf{x}) = \lVert \mathbf{x} \rVert \, \kappa(Z^\top Z)^{-1/2} \, \kappa\!\left(Z^\top \mathbf{x} / \lVert \mathbf{x} \rVert \right).
$$

The extra factor \(\kappa(Z^\top Z)^{-1/2}\) is precisely the projection onto the subspace \(\mathcal{F}\), the geometric content that distinguishes learning a subspace of an RKHS from applying an arbitrary nonlinearity. Everything downstream flows from this one change. We inherit a geometric interpretation of the filters as anchor points defining a subspace, an unsupervised training route through K-means, a still-feasible supervised route through backpropagation, and regularization mechanisms from the kernel viewpoint. Because the representation is a genuine kernel embedding, it can in principle be reused in other kernel machinery: statistical testing, kernel PCA, CCA, K-means.

On CIFAR-10 the construction is competitive rather than merely illustrative. A 14-layer supervised model with 256 anchor points per layer reaches about 92% accuracy with only mild data augmentation, vanilla SGD with momentum, and no batch normalization. A two-layer unsupervised model with a very large number of anchor points, from 1024 up to 16384, and no augmentation reaches about 86%, and its accuracy increases monotonically with the number of anchors, because more anchors mean a better kernel approximation. Computing the exact kernel is impractical but feasible on many CPUs, and a three-layer exact model reaches about 90% (unpublished, by A. Bietti), consistent with the findings of Shankar et al. (2020). Related lines of work connect kernels to deep learning through hierarchical kernel descriptors (Bo et al., 2011), other multilayer models (Bouvrie et al., 2009; Montavon et al., 2011; Anselmi et al., 2015), deep Gaussian processes (Damianou and Lawrence, 2013), multilayer PCA (Schölkopf et al., 1998), old image kernels (Schölkopf, 1997) that amount to a one-layer CKN, and RBF networks (Broomhead and Lowe, 1988).

## Deep learning and stability to deformations {#stability}

We now cash in the promise that \(\Phi\) can be analyzed independently of the data. The results are those of Bietti and Mairal (2019a): a multilayer construction of the RKHS \(\mathcal{H}\) that contains convolutional networks with smooth homogeneous activations; conditions under which the feature map \(\Phi\) preserves the signal, is stable to deformations, and is non-expansive; constructions achieving invariance to groups beyond translation; and bounds on the RKHS norm \(\lVert \cdot \rVert_{\mathcal{H}}\) that control both stability and generalization of a learned model, through the same inequality

$$
\lvert f(\mathbf{x}) - f(\mathbf{x}') \rvert \leq \lVert f \rVert_{\mathcal{H}} \, \lVert \Phi(\mathbf{x}) - \Phi(\mathbf{x}') \rVert_{\mathcal{H}}.
$$

### Smooth homogeneous activations {#smooth-homogeneous}

For a network to live inside this RKHS, its activation must be admissible, and the plain ReLU is not: it is not smooth at the origin. The fix is to homogenize. Instead of \(\mathbf{z} \mapsto \operatorname{ReLU}(\mathbf{w}^\top \mathbf{z})\), the kernel construction uses

$$
\mathbf{z} \; \longmapsto \; \lVert \mathbf{z} \rVert \, \sigma\!\left(\mathbf{w}^\top \mathbf{z} / \lVert \mathbf{z} \rVert \right),
$$

where \(\sigma\) is a smooth surrogate of the ReLU (a \"sReLU\"). The factor \(\lVert \mathbf{z} \rVert\) restores the positive homogeneity that the ReLU has and that a bare smooth function would lack, so that the map is homogeneous of degree one, like the original neuron, while being differentiable everywhere the analysis needs.

### Deformations and the notion of stability {#deformation-definition}

A deformation is a \(C^1\)-diffeomorphism \(\tau : \Omega \to \Omega\) acting on signals through

$$
L_\tau \mathbf{x}(u) = \mathbf{x}(u - \tau(u)).
$$

This is a far richer group of transformations than pure translations \(L_c \mathbf{x}(u) = \mathbf{x}(u - c)\): patterns are not only shifted but locally warped. The right notion of robustness, following Mallat's (2012) analysis of the scattering transform (Bruna and Mallat, 2013), asks the representation to change little under small deformations.

:::: {.definition #def-39-8}
[Definition (stability, Mallat, 2012)]{.box-title}

A representation \(\Phi\) is stable to deformations if there are constants \(C_1, C_2\) such that

$$
\lVert \Phi(L_\tau \mathbf{x}) - \Phi(\mathbf{x}) \rVert \leq \left(C_1 \lVert \nabla\tau \rVert_\infty + C_2 \lVert \tau \rVert_\infty \right) \lVert \mathbf{x} \rVert,
$$

where \(\lVert \nabla\tau \rVert_\infty = \sup_u \lVert \nabla\tau(u) \rVert\) measures the size of the deformation and \(\lVert \tau \rVert_\infty = \sup_u \lvert \tau(u) \rvert\) measures the maximal displacement.
::::

The second term is a translation term; driving \(C_2 \to 0\) yields translation invariance. The goal is a representation for which \(C_1\) is small (genuine deformation stability) and \(C_2\) can be made small at will (tunable invariance).

### Warmup: translation invariance {#translation-invariance}

Pure translation is instructive because every operator in the network is *equivariant* to it: patch extraction, the pointwise nonlinear map, and pooling all commute with a shift, \(\square L_c = L_c \square\). Therefore the shifted representation is the shift of the representation, and the whole error collapses onto the last pooling layer:

$$
\lVert \Phi_n(L_c \mathbf{x}) - \Phi_n(\mathbf{x}) \rVert
= \lVert L_c \Phi_n(\mathbf{x}) - \Phi_n(\mathbf{x}) \rVert
\leq \lVert L_c A_n - A_n \rVert \cdot \lVert M_n P_n \Phi_{n-1}(\mathbf{x}) \rVert
\leq \lVert L_c A_n - A_n \rVert \, \lVert \mathbf{x} \rVert.
$$

Mallat's estimate on how much a shift disturbs a Gaussian pooling of scale \(\sigma_n\) is \(\lVert L_c A_n - A_n \rVert \leq (C_2 / \sigma_n)\, c\). So the last layer's pooling scale \(\sigma_n\) directly controls translation invariance: the coarser the final pooling, the more shift-invariant the representation. This is the mechanism a global average-pooling layer exploits.

### Stability to deformations {#deformation-stability}

General deformations are harder because patch extraction \(P_k\) and pooling \(A_k\) do *not* commute with \(L_\tau\); only the pointwise map \(M_k\) does. The disturbance is measured by commutators. For pooling, Mallat's bound gives

$$
\lVert [A_k, L_\tau] \rVert = \lVert A_k L_\tau - L_\tau A_k \rVert \leq C_1 \lVert \nabla\tau \rVert_\infty,
$$

which is well behaved. Patch extraction is the problem: \([P_k, L_\tau]\) is unstable at high frequencies, because a fixed patch operator resolves detail finer than the current feature map can safely carry under a warp. The remedy is to adapt the patch to the current resolution. If the patch of layer \(k\) is applied after the pooling of layer \(k-1\), so that its size is controlled by \(\sigma_{k-1}\), the commutator is tamed,

$$
\lVert [P_k A_{k-1}, L_\tau] \rVert \leq C_{1,\kappa} \lVert \nabla\tau \rVert_\infty,
\qquad \sup_{u \in S_k} \lvert u \rvert \leq \kappa \, \sigma_{k-1},
$$

where \(\kappa\) is the ratio of patch size to resolution. The constant grows as \(C_{1,\kappa} = O(\kappa^{d+1})\), so small patches relative to the resolution are more stable. This is a first-principles reason to prefer the small 3-by-3 patches of VGG-style architectures. Assembling the layerwise bounds yields the main theorem.

:::: {.theorem #thm-39-9}
[Theorem (stability of CKNs, Bietti and Mairal, 2019a)]{.box-title}

Let \(\Phi_n(\mathbf{x}) = \Phi(A_0 \mathbf{x})\) and assume \(\lVert \nabla\tau \rVert_\infty \leq 1/2\). Then

$$
\lVert \Phi_n(L_\tau \mathbf{x}) - \Phi_n(\mathbf{x}) \rVert \leq \left(C \beta \, (n+1) \lVert \nabla\tau \rVert_\infty + \frac{C}{\sigma_n} \lVert \tau \rVert_\infty \right) \lVert \mathbf{x} \rVert.
$$

**Assumptions.** All domains, quantifiers, regularity conditions, and parameter restrictions stated in the result are in force; no converse is implied.
**Proof status.** No separate proof is attached to this result; independent technical review must determine whether the surrounding derivation is sufficient.
::::

The bound reads as a design guide. Translation invariance comes from a large final scale \(\sigma_n\), which shrinks the second term. Deformation stability comes from small patches, since \(\beta\) is the patch size and the deformation constant behaves as \(C_\beta = O(\beta^3)\) for images. Signal preservation requires the subsampling factor to be roughly the patch size. Because each layer can only subsample by about \(\beta\), reaching a final scale \(\sigma_n\) from an initial \(\sigma_0\) needs several layers,

$$
n = O\!\left(\frac{\log(\sigma_n / \sigma_0)}{\log \beta} \right).
$$

Small patches force depth: the network must be deep precisely because we insisted on small, stable patches. The proof rests on controlling the commutator norm \(\lVert [L_\tau, P_k A_{k-1}] \rVert\), extending Mallat's (2012) control of \(\lVert [L_\tau, A] \rVert\), and requires patches \(S_k\) adapted to the resolution, \(\operatorname{diam} S_k \leq \beta \sigma_{k-1}\).

### Invariance to other groups {#beyond-translation}

Nothing above is special to translations. To build invariance to a locally compact group \(G\) (rotations, reflections), let the group act by \(L_g \mathbf{x}(u) = \mathbf{x}(g^{-1} u)\) and define feature maps on \(G\) itself. The recipe is equivariant inner layers followed by a global pooling in the last layer. Patch extraction becomes \(P\mathbf{x}(u) = (\mathbf{x}(uv))_{v \in S}\); the nonlinear map is equivariant automatically because it is pointwise; and pooling integrates against the left-invariant Haar measure \(\mu\),

$$
A\mathbf{x}(u) = \int_G \mathbf{x}(uv) \, h(v) \, d\mu(v) = \int_G \mathbf{x}(v) \, h(u^{-1} v) \, d\mu(v).
$$

This recovers and generalizes group-equivariant constructions (Sifre and Mallat, 2013; Cohen and Welling, 2016; Raj et al., 2016). The same analysis extends to the neural tangent kernel of infinitely wide convolutional networks (Bietti and Mairal, 2019b), which is stable too, with the weaker rate

$$
\lVert \Phi_n(L_\tau \mathbf{x}) - \Phi_n(\mathbf{x}) \rVert \leq \left(C \beta \, n^{7/4} \lVert \nabla\tau \rVert_\infty^{1/2} + C' \beta \, n^2 \lVert \nabla\tau \rVert_\infty + \sqrt{n+1}\, \frac{C}{\sigma_n} \lVert \tau \rVert_\infty \right) \lVert \mathbf{x} \rVert.
$$

The half-power on \(\lVert \nabla\tau \rVert_\infty\) makes the convolutional NTK less deformation-stable than the CKN, a concrete difference between the lazy-training kernel and the architectural kernel.

## Discretization and signal preservation {#discretization}

We promised that subsampling loses no information as long as its factor stays below the patch size, and we now show why in one dimension, because it explains what the \"filters\" of a CKN really encode. Write \(\bar{\mathbf{x}}_k \in \ell^2(\mathbb{Z}, \bar{\mathcal{H}}_k)\) for the discrete feature maps, obtained from the continuous \(\mathbf{x}_k \in L^2(\mathbb{R}, \mathcal{H}_k)\) by subsampling with factor \(s_k\) after pooling at a matching scale \(\sigma_k \approx s_k\),

$$
\bar{\mathbf{x}}_k[n] = \bar{A}_k \bar{M}_k \bar{P}_k \bar{\mathbf{x}}_{k-1}[n s_k].
$$

::: {.proposition #prop-39-10}
[Claim (recovery)]{.box-title}

If \(s_k\) is at most the patch size, then \(\bar{\mathbf{x}}_{k-1}\) can be recovered from \(\bar{\mathbf{x}}_k\).

**Assumptions.** All domains, quantifiers, regularity conditions, and parameter restrictions stated in the result are in force; no converse is implied.
**Proof status.** No separate proof is attached to this result; independent technical review must determine whether the surrounding derivation is sufficient.
:::

The mechanism is that the RKHS \(\bar{\mathcal{H}}_k\) contains the linear functionals of a patch, so we can read the patch back out of its embedding. For a linear function \(f_\mathbf{w}\), evaluating it against the embedded patch recovers a linear measurement of the raw patch,

$$
\langle f_\mathbf{w}, \bar{M}_k \bar{P}_k \bar{\mathbf{x}}_{k-1}(u) \rangle = f_\mathbf{w}\!\left(\bar{P}_k \bar{\mathbf{x}}_{k-1}(u) \right) = \langle \mathbf{w}, \bar{P}_k \bar{\mathbf{x}}_{k-1}(u) \rangle,
$$

and running \(\mathbf{w}\) over an orthonormal basis \(\mathcal{B}\) of patch space reconstructs the whole patch,

$$
\bar{P}_k \bar{\mathbf{x}}_{k-1}(u) = \sum_{\mathbf{w} \in \mathcal{B}} \langle f_\mathbf{w}, \bar{M}_k \bar{P}_k \bar{\mathbf{x}}_{k-1}(u) \rangle \, \mathbf{w}.
$$

Given the patches at every location one can deconvolve the pooling and recover \(\bar{\mathbf{x}}_{k-1}\), provided the subsampling was not so aggressive as to skip patches, hence the \"factor at most patch size\" condition. One warning: this is an information-theoretic statement, with no claim that the recovery is practical or numerically stable. It is the pre-image of the filters in principle, not a recommended algorithm.

## What the patch RKHS contains, and CNNs inside it {#rkhs-content}

Stability told us about the map \(\Phi\); to talk about the learned model \(f\) we need to know which functions live in the RKHS and what their norms are. For the homogeneous patch kernel \(K_k(\mathbf{z}, \mathbf{z}') = \lVert \mathbf{z} \rVert \lVert \mathbf{z}' \rVert \kappa(\langle \mathbf{z}, \mathbf{z}' \rangle / (\lVert \mathbf{z} \rVert \lVert \mathbf{z}' \rVert))\) with \(\kappa(u) = \sum_{j \geq 0} b_j u^j\), the RKHS contains all homogeneous functions of the form

$$
f : \mathbf{z} \; \longmapsto \; \lVert \mathbf{z} \rVert \, \sigma\!\left(\langle \mathbf{g}, \mathbf{z} \rangle / \lVert \mathbf{z} \rVert \right),
$$

for any smooth activation \(\sigma(u) = \sum_{j \geq 0} a_j u^j\) with non-negative coefficients, exactly the homogenized neurons of the stability analysis. Their norm is bounded through the coefficients of \(\sigma\) and \(\kappa\),

$$
\lVert f \rVert_{\mathcal{H}_k}^2 \leq C_\sigma^2(\lVert \mathbf{g} \rVert^2) = \sum_{j=0}^\infty \frac{a_j^2}{b_j} \lVert \mathbf{g} \rVert^{2j} \lt \infty,
$$

a homogeneous version of the analysis of Zhang et al. (2016, 2017). The rate at which \(C_\sigma^2\) grows measures how expensive the activation is to represent: \(\sigma(u) = u\) (linear) gives \(C_\sigma^2(\lambda^2) = O(\lambda^2)\); a degree-\(p\) polynomial gives \(O(\lambda^{2p})\); and smoother saturating activations such as the sine, the sigmoid, or a smooth ReLU cost \(O(e^{c\lambda^2})\), an exponential price for their smoothness.

This is the last ingredient needed for the linearization principle. A convolutional network with filters \(W_k^{ij}(u)\) (layer \(k\), output filter \(i\), input channel \(j\)) and smooth homogeneous activations can be built hierarchically inside \(\mathcal{H}_K\), so that

$$
f(\mathbf{x}) = \sigma_k\!\left(A_k \sigma_{k-1}(A_{k-1} \cdots \sigma_1(A_1 \mathbf{x}) \cdots) \right) = \langle f, \Phi(\mathbf{x}) \rangle_{\mathcal{H}}.
$$

The RKHS norm of the network is bounded by nesting the activation-cost function through the layers,

$$
\lVert f_\sigma \rVert^2 \leq \lVert W_{n+1} \rVert_2^2 \, C_\sigma^2\!\left(\lVert W_n \rVert_2^2 \, C_\sigma^2\!\left(\lVert W_{n-1} \rVert_2^2 \, C_\sigma^2(\cdots) \right) \right),
$$

and in the purely linear case (\(\sigma = \text{id}\)) this collapses to the product of spectral norms of the layers,

$$
\lVert f_\sigma \rVert^2 \leq \lVert W_{n+1} \rVert_2^2 \cdot \lVert W_n \rVert_2^2 \cdots \lVert W_1 \rVert_2^2.
$$

### Link with generalization {#generalization}

Because the trained CNN is now a function in an RKHS ball, the classical generalization theory of [[ch:learning-theory]] applies with no modification. For the ball \(\mathcal{F}_B = \{ f \in \mathcal{H}_K : \lVert f \rVert \leq B \}\), the Rademacher complexity over \(N\) samples of radius \(R\) is

$$
\operatorname{Rad}_N(\mathcal{F}_B) \leq O\!\left(\frac{B R}{\sqrt{N}} \right),
$$

which yields a margin bound \(O(\lVert \hat{f}_N \rVert R / (\gamma \sqrt{N}))\) for a learned CNN \(\hat{f}_N\) with confidence margin \(\gamma \gt 0\). Since \(\lVert \hat{f}_N \rVert\) is controlled by the product of spectral norms, this recovers, from the kernel viewpoint, the spectral-norm generalization bounds for neural networks of Bartlett et al. (2017) and Neyshabur et al. (2018), and connects to the classical treatments of Boucheron et al. (2005) and Shalev-Shwartz and Ben-David (2014).

The conclusion of the convolutional story is a single unifying observation: the same quantity \(\lVert f \rVert\) controls both stability and generalization. Deformation stability comes from small patches adapted to the resolution, signal preservation from subsampling below the patch size, and group invariance from the choice of patch extraction and pooling. The tension is real, since higher capacity, a larger \(\lVert f \rVert\), is exactly what is needed to discriminate small deformations, which is why regularization is delicate. Open questions remain: what is the right regularizer, how does SGD implicitly control capacity in CNNs, and what happens in networks without pooling such as ResNets.

## Application to graphs: graph convolutional kernel networks {#graphs}

Images are not the only data with local structure. Molecules, protein interaction maps, social networks, and chemical pathways are graphs, and the CKN recipe (extract local patterns, embed with a kernel, pool) transfers to them. The state of the art has three camps: deep learning for graphs, that is graph neural networks (GNNs); graph kernels, notably the Weisfeiler-Lehman (WL) subtree kernel; and hybrids such as the graph neural tangent kernel (GNTK). Graph convolutional kernel networks (GCKN) unify them: a multilayer graph kernel that is more expressive than WL, that yields easy-to-regularize and scalable unsupervised representations, and that can also be trained supervised like a GNN.

The reason the transfer works at all is that the CKN recipe never referred to the grid. Patch extraction only needed a notion of local neighborhood, kernel embedding only needed a similarity between local patterns, and pooling only needed a way to aggregate over positions. Images supply these through boxes of pixels, but a graph supplies them just as well through the paths leaving a node, and once the three moves are named abstractly the same map into an RKHS is available on either domain. So the throughline survives the change of data: a graph convolutional kernel network is again a feature map \(\Phi\) sending a graph into an RKHS, and a prediction is again a linear functional of it. What changes is only the local pattern the first move extracts.

A graph is a triple \(G = (\mathcal{V}, \mathcal{E}, a)\) with vertices \(\mathcal{V}\), edges \(\mathcal{E}\), and an attribute function \(a : \mathcal{V} \to \mathbb{R}^d\) assigning a feature vector to each node. A large class of graph kernels represents a graph by summing a base embedding of a local pattern \(\ell_G(u)\) over its nodes,

$$
\Phi(G) = \sum_{u \in \mathcal{V}} \varphi_{\text{base}}(\ell_G(u)),
\qquad
K(G, G') = \sum_{u \in \mathcal{V}} \sum_{u' \in \mathcal{V}'} \langle \varphi_{\text{base}}(\ell_G(u)), \varphi_{\text{base}}(\ell_{G'}(u')) \rangle = \sum_{u} \sum_{u'} \kappa_{\text{base}}(\ell_G(u), \ell_{G'}(u')),
$$

comparing every local pattern of \(G\) with every one of \(G'\) (Shervashidze et al., 2011; Lei et al., 2017; Kriege et al., 2020).

### From the path kernel to its relaxation {#relaxed-path-kernel}

The local pattern we build on is the *path*. Paths are more expressive than walks, because a walk may repeat vertices while a path may not, though paths are costlier to enumerate. Let \(\mathcal{P}_k(G, u)\) be the set of paths of length \(k\) starting at node \(u\). The \(k\)-path embedding places a Dirac spike at the concatenated attributes \(a(p)\) of each path,

$$
\varphi_{\text{path}}(u) = \sum_{p \in \mathcal{P}_k(G, u)} \delta_{a(p)}(\cdot)
\quad\Longrightarrow\quad
\Phi(G) = \sum_{u \in \mathcal{V}} \sum_{p \in \mathcal{P}_k(G, u)} \delta_{a(p)},
$$

so \(\Phi(G)\) is a histogram of path occurrences. This has two defects. The Dirac \(\delta\) only makes hard, all-or-nothing comparisons, so it handles discrete attributes but not continuous ones; and it is not differentiable, so it cannot be optimized by backpropagation. Both defects vanish if we soften the spike into a Gaussian bump,

$$
\varphi_{\text{path}}(u) = \sum_{p \in \mathcal{P}_k(G, u)} \delta_{a(p)}(\cdot)
\quad\longrightarrow\quad
\sum_{p \in \mathcal{P}_k(G, u)} e^{-\frac{\alpha}{2}\lVert a(p) - \cdot \rVert^2},
$$

a sum of Gaussians centered at each path's attributes: soft, differentiable, and defined for continuous attributes.

### One layer of GCKN {#one-layer-gckn}

The one-layer GCKN is exactly this relaxed path kernel mapping,

$$
\varphi_1(u) := \sum_{p \in \mathcal{P}_k(G, u)} e^{-\frac{\alpha_1}{2}\lVert a(p) - \cdot \rVert^2} = \sum_{p \in \mathcal{P}_k(G, u)} \varphi_{\mathrm{RBF}}(a(p)) \; \in \; \mathcal{H}_1,
$$

and it decomposes into the same three moves as a CKN layer. *Path extraction*: enumerate all paths \(\mathcal{P}_k(G, u)\) from \(u\), the graph analogue of patch extraction. *Kernel mapping*: embed each path's features through the Gaussian feature map \(\varphi_{\mathrm{RBF}}\). *Path aggregation*: sum the path embeddings, the graph analogue of pooling. The result is a new graph on the same topology but with richer node features,

$$
(\mathcal{V}, \mathcal{E}, a) \xrightarrow{\;\varphi_{\text{path}}\;} (\mathcal{V}, \mathcal{E}, \varphi_1).
$$

### Stacking layers {#multilayer-gckn}

Because the output is again an attributed graph, we can apply \(\varphi_{\text{path}}\) again,

$$
(\mathcal{V}, \mathcal{E}, a) \xrightarrow{\;\varphi_{\text{path}}\;} (\mathcal{V}, \mathcal{E}, \varphi_1) \xrightarrow{\;\varphi_{\text{path}}\;} (\mathcal{V}, \mathcal{E}, \varphi_2) \xrightarrow{\;\varphi_{\text{path}}\;} \cdots \xrightarrow{\;\varphi_{\text{path}}\;} (\mathcal{V}, \mathcal{E}, \varphi_j),
$$

and read out the graph representation at layer \(j\) as \(\Phi(G) = \sum_{u \in \mathcal{V}} \varphi_j(u)\). Depth buys structure: applying \(\varphi_{\text{path}}\) once captures paths (GCKN-path), applying it twice captures subtrees (GCKN-subtree), and applying it more may capture higher-order substructures. This is how the model reaches long-range structure without ever enumerating long paths, which would be computationally prohibitive; a stack of short-path layers captures long-range substructures that a single layer could not afford.

### Scalable approximation {#gckn-scalable}

The Gaussian embedding \(\varphi_{\mathrm{RBF}}(a(p)) = e^{-\frac{\alpha}{2}\lVert a(p) - \cdot \rVert^2}\) is infinite-dimensional, so we apply Nyström exactly as for images. Project \(\varphi_{\mathrm{RBF}}(a(p))\) onto \(\operatorname{span}(\varphi_{\mathrm{RBF}}(z_1), \ldots, \varphi_{\mathrm{RBF}}(z_q))\), a subspace parametrized by anchors \(Z = \{z_1, \ldots, z_q\}\), giving a finite embedding \(\Psi(a(p)) \in \mathbb{R}^q\). Here the anchors \(z_j \in \mathbb{R}^{d_k}\) have a concrete meaning: they are learned *path features*, prototypical local patterns. As before, \(Z\) can be fit unsupervised by K-means on the observed path features, or supervised by end-to-end backpropagation (Chen et al., 2019a,b; Williams and Seeger, 2001).

### GCKN versus GNN {#gckn-vs-gnn}

The comparison to a graph neural network is now transparent, since both sum a local aggregation over nodes,

$$
f_{\mathrm{GCKN}}(G) = \sum_{u \in G} \psi_k(u),
\qquad
f_{\mathrm{GNN}}(G) = \sum_{u \in G} f_k(u),
$$

but they aggregate differently:

$$
\psi_k(u) = \sum_{p \in \mathcal{P}_k(G, u)} \kappa(Z^\top Z)^{-1/2} \, \kappa(Z^\top \psi_{k-1}(p)),
\qquad
f_k(u) = \sum_{v \in \mathcal{N}(u)} \operatorname{ReLU}(Z^\top f_{k-1}(v)).
$$

                 GCKN                          GNN
  -------------- ----------------------------- ------------------------------
  aggregation    local path aggregation        neighborhood aggregation
  nonlinearity   projection in a known RKHS    ReLU, no RKHS interpretation
  training       supervised and unsupervised   supervised only

The GCKN aggregates over paths rather than immediate neighbors, and its nonlinearity \(\kappa(Z^\top Z)^{-1/2} \kappa(Z^\top \cdot)\) is a projection onto a known subspace of an RKHS, which is what gives it an unsupervised training mode and a built-in regularization theory that the GNN's bare ReLU lacks.

### Experiments and interpretation {#gckn-experiments}

On benchmark graphs with discrete attributes (MUTAG, PROTEINS, PTC, NCI1, IMDB-B, IMDB-M, COLLAB), measured as accuracy improvement over the WL subtree kernel and against GNTK, GCN, and GIN, GCKN-path already outperforms the baselines, adding layers brings larger improvements, and supervised training does not raise accuracy over unsupervised but yields more compact representations. On graphs with continuous attributes (ENZYMES, PROTEINS, BZR, COX2), compared to the WWL kernel and GNTK, the picture is the same, suggesting path features are already predictive enough (Shervashidze et al., 2011; Du et al., 2019; Xu et al., 2019; Kipf and Welling, 2017; Togninalli et al., 2019).

Because a GCKN is a kernel model, it is also interpretable. For mutagenicity prediction one can ask for the minimal connected subgraph that preserves the model's prediction (in the spirit of Ying et al., 2019). On a mutagenic molecule this minimal component isolates the chemical substructure responsible for the classification, so the model does not merely predict mutagenicity but points at the fragment driving the prediction, closing the loop between accuracy and explanation that motivated the kernel viewpoint.

## Application to biological sequences {#sequences}

The third domain is biological sequences, where the same recipe produces convolutional and recurrent kernel networks. The task is standard supervised learning: given sequences \(\mathbf{x}_1, \ldots, \mathbf{x}_n \in \mathcal{X}\) with labels \(y_1, \ldots, y_n\), learn a predictive and *interpretable* function by regularized risk minimization,

$$
\min_{f \in \mathcal{F}} \; \frac{1}{n} \sum_{i=1}^n L(y_i, f(\mathbf{x}_i)) + \mu \, \Omega(f),
$$

and the modeling question is how to choose \(\mathcal{F}\). The classical answer for a sequence over an alphabet \(\mathcal{A}\) is a string kernel comparing counts of \(k\)-mers,

$$
K(\mathbf{x}, \mathbf{x}') = \sum_{u \in \mathcal{A}^k} \delta_u(\mathbf{x}) \, \delta_u(\mathbf{x}') = \langle \Phi(\mathbf{x}), \Phi(\mathbf{x}') \rangle,
$$

where \(\delta_u(\mathbf{x})\) counts occurrences of the \(k\)-mer \(u\): exact occurrences (spectrum kernel, Leslie et al., 2002), occurrences up to \(m\) mismatches (mismatch kernel, Leslie and Kuang, 2004), or occurrences with gaps weighted by an exponential decay (substring kernel, Lodhi et al., 2002). The feature map \(\Phi(\mathbf{x})\) is a histogram of pattern occurrences, and its discreteness is the same obstacle we met with paths.

### A convolutional kernel network for sequences {#ckn-sequences}

We relax the mismatch kernel to a continuous, differentiable one (Chen et al., 2019a; Morrow et al., 2017) by comparing all pairs of \(k\)-mers through a smooth kernel \(K_0\) rather than exact matching,

$$
K_{\mathrm{CKN}}(\mathbf{x}, \mathbf{x}') = \sum_{i=1}^{|\mathbf{x}|-k+1} \sum_{j=1}^{|\mathbf{x}'|-k+1} K_0\!\left(\mathbf{x}[i{:}i{+}k], \, \mathbf{x}'[j{:}j{+}k] \right) = \Big\langle \sum_i \varphi_0(\mathbf{x}[i{:}i{+}k]), \; \sum_j \varphi_0(\mathbf{x}'[j{:}j{+}k]) \Big\rangle.
$$

Each \(k\)-mer is one-hot encoded as a matrix in \(\mathbb{R}^{k \times d}\), for instance the DNA window TTGAG over the alphabet \(\{A, T, C, G\}\) becomes a \(4 \times 5\) binary matrix with a single 1 per column, and \(K_0\) is a Gaussian kernel over these encodings. The mapping \(\Phi(\mathbf{x}) = \sum_i \varphi_0(\mathbf{x}[i{:}i{+}k])\) is a differentiable relaxation of the mismatch kernel.

To make it finite we again use Nyström, and here it is worth stating the general form. Projecting \(\varphi_0(u)\) onto \(E_0 = \operatorname{span}(\varphi_0(z_1), \ldots, \varphi_0(z_q))\) gives \(\psi_0(u) \in \mathbb{R}^q\) with

$$
\psi_0(u) = K_0(Z, Z)^{-1/2} K_0(Z, u),
$$

and when \(K_0(u, u') = \kappa(\langle u, u' \rangle)\) is a dot-product kernel this specializes to the by-now familiar layer

$$
\psi_0(u) = \kappa(Z^\top Z)^{-1/2} \kappa(Z^\top u),
$$

a linear operation, a pointwise nonlinearity \(\kappa\), and a linear operation, with \(\kappa(\beta) = e^{\beta - 1}\), polynomial, inverse polynomial, or arc-cosine. A single-layer CKN for sequences extracts \(k\)-mers, applies \(\psi_0\), pools globally into \(\Psi(\mathbf{x}) \in \mathbb{R}^q\), and predicts with \(\langle \mathbf{w}, \Psi(\mathbf{x}) \rangle\); stacking an embedding layer, a convolutional kernel layer with a dot-product kernel, and pooling gives the multilayer version.

### Gapped k-mers and recurrent kernel networks {#rkn}

Biological motifs tolerate insertions, so we want to compare \(k\)-mers *with gaps*. For a sequence \(\mathbf{x} = x_1 \ldots x_n\) and an ordered index tuple \(\imath = (i_1, \ldots, i_k)\), define the \(k\)-substring \(\mathbf{x}[\imath] = x_{i_1} x_{i_2} \ldots x_{i_k}\) and let \(\operatorname{gaps}(\imath)\) count the gaps it skips. For example, in \(\mathbf{x} = \texttt{ABRACADABRA}\) the indices \(\imath = (3,4,7,8,10)\) read off \(\mathbf{x}[\imath] = \texttt{RADAR}\) with \(\operatorname{gaps}(\imath) = 3\). The recurrent kernel network compares all gapped \(k\)-mers, penalizing gaps geometrically with a factor \(\lambda \in (0,1)\),

$$
K_{\mathrm{RKN}}(\mathbf{x}, \mathbf{x}') = \sum_{\imath \in \mathcal{I}(k, |\mathbf{x}|)} \sum_{\jmath \in \mathcal{I}(k, |\mathbf{x}'|)} \lambda^{\operatorname{gaps}(\imath)} \lambda^{\operatorname{gaps}(\jmath)} K_0\!\left(\mathbf{x}[\imath], \mathbf{x}'[\jmath] \right),
\qquad
\Phi(\mathbf{x}) = \sum_{\imath \in \mathcal{I}(k, |\mathbf{x}|)} \lambda^{\operatorname{gaps}(\imath)} \varphi_0(\mathbf{x}[\imath]),
$$

a differentiable relaxation of the substring kernel. Enumerating all index tuples is exponentially costly, but the Nyström feature map

$$
\Psi(\mathbf{x}) = \sum_{\imath \in \mathcal{I}(k, t)} \lambda^{\operatorname{gaps}(\imath)} \psi_0(\mathbf{x}[\imath]) \in \mathbb{R}^q,
\qquad
\psi_0(\mathbf{x}[\imath]) = \kappa(Z^\top Z)^{-1/2} \kappa(Z^\top \mathbf{x}[\imath]),
$$

can be computed by dynamic programming (Lodhi et al., 2002), and the resulting recursion is precisely a particular recurrent neural network (Lei et al., 2017). When \(K_0\) is Gaussian, the RKN feature map is a mixture of Gaussians centered at the substrings \(\mathbf{x}[\imath]\) and weighted by \(\lambda^{\operatorname{gaps}(\imath)}\): the network is a soft, gap-tolerant substring counter that trains by gradient descent.

On protein fold classification (SCOP 2.06; Hou et al., 2017), using informative sequence features such as PSSM profiles, secondary structure, and solvent accessibility, these models are competitive with or better than deep baselines at a fraction of the parameters. A CKN with 512 filters (843k parameters) reaches about 84% top-1 and 94% top-5 accuracy, and the recurrent RKN with 512 filters matches its parameter budget while improving to about 85% top-1 and 95% top-5, with a marked gain on the hardest fold-level stratum, where gap tolerance matters most (Chen et al., 2019a).

::: {.remark}
[Further reading]{.box-title}

Almost everything in this chapter postdates the two standard kernel-methods textbooks, so it is not covered by either of them and the primary literature is the place to go. For the neural tangent kernel and the sense in which wide networks train like kernel machines, the original reference is Jacot, Gabriel, and Hongler (2018), with the lazy-training mechanism isolated by Chizat, Oyallon, and Bach (2019). For convolutional kernel networks and the idea of designing an RKHS in which a whole convolutional network lives as a single linear functional, learned end to end through the anchor points, see Mairal (2016). For the analysis of what that representation guarantees, the invariance and stability of deep convolutional representations to translations and to smooth deformations, the reference is Bietti and Mairal (2019). The random-feature side traces back to Neal (1996) for the Gaussian-process view and to Cho and Saul (2009) for the arc-cosine kernels, while the graph and sequence constructions are developed by Chen, Jacob, and Mairal (2020) and the works cited there.
:::

## Why a fixed kernel makes a poor hidden neuron {#kernel-neuron-failure}

Everything above puts kernels around deep networks: as their infinite-width limit, as their training dynamics, as the RKHS that contains them. It is worth asking the blunter question the history of the field already answered the hard way. Why not use a kernel *as* the neuron itself, a hidden unit \(x \mapsto k(x, c)\) with a trainable center \(c\), stack those into layers, and train the whole thing by gradient descent the way we train a network of rectifiers? Radial basis function networks (Broomhead and Lowe, 1988) did exactly this with the Gaussian unit \(\phi_c(x) = \exp(-\lVert x - c\rVert^2 / 2\sigma^2)\), and for a shallow width-one-layer model with a linear readout they work well. As a deep, end-to-end, gradient-trained building block the Gaussian neuron failed, and it failed for reasons that say something precise about what a hidden unit has to do.

### The activation, and its gradient, vanish almost everywhere {#vanishing-signal}

A rectifier \(\max(0, w^\top x + b)\) has a gradient that is either zero or one; on its active half-space the signal passes through undiminished no matter how far the input sits from the boundary. A Gaussian unit is the opposite. Away from its center it is not merely small, it is exponentially flat: both the activation and its derivatives decay like \(\exp(-\lVert x - c\rVert^2 / 2\sigma^2)\). The gradient that would move the center toward the data,

$$ \frac{\partial \phi_c(x)}{\partial c} = \frac{x - c}{\sigma^2}\, \phi_c(x), $$

carries the same vanishing factor \(\phi_c(x)\). So a unit whose center starts far from a training point receives an almost-zero learning signal about that point, and in high dimensions *every* randomly placed center starts far from *every* point, because volume concentrates away from any fixed location. The network begins in a dead state and gradient descent has almost no gradient with which to leave it. A rectifier saturates on one side only; a Gaussian saturates on all sides at once.

::: {.remark}
[The tell in the training recipe]{.box-title}

Classical RBF networks were almost never trained end to end. The standard recipe placed the centers first by an unsupervised method, k-means over the inputs, and only then fitted the linear readout, which is a convex problem the representer theorem hands you for free. That two-stage split is precisely an admission that gradient descent on the centers does not work: the placement had to be solved by other means because the vanishing signal would not solve it.
:::

### Locality does not compose the way piecewise-linearity does {#locality-curse}

The deeper obstacle is representational. A Gaussian unit is a *local* feature: it responds to a bounded neighborhood and is blind everywhere else. A network of local features is a memory-based method in disguise, and to cover a region of a \(d\)-dimensional input space with bumps of width \(\sigma\) takes on the order of \((L/\sigma)^d\) of them. Bengio, Delalleau, and Le Roux (2006) made this exact, showing that local kernel machines need a number of examples, and of units, that grows with the number of variations of the target function, so a function that turns over many times cannot be represented compactly by any shallow bank of kernels. Depth is supposed to be the escape, but stacking Gaussian layers composes local bumps of local bumps and the result stays local, while the vanishing-gradient problem compounds at every layer. Compose piecewise-linear maps instead and the number of linear regions grows exponentially in depth (Montufar, Pascanu, Cho, and Bengio, 2014): the rectifier buys expressivity from depth precisely where the Gaussian does not.

### Bandwidth is a knife-edge, and scale is unmanaged {#bandwidth-scale}

There is also a conditioning problem that gradient descent handles badly. The bandwidth \(\sigma\) sits on a knife-edge: too small and every unit sees only its own center, the Gram matrix approaches the identity, and there is no signal to share between points; too large and all units respond almost identically, the Gram matrix collapses toward rank one, and there is nothing to discriminate. The usable band is narrow and data-dependent, and because each layer rescales distances, a single fixed \(\sigma\) cannot stay in that band through a deep stack. Rectifier networks have a matching answer for scale in their initialization, the variance-preserving schemes of Glorot and Bengio (2010) and their successors, which keep activations and gradients at unit scale across many layers. There is no comparable initialization that keeps a stack of Gaussian units alive at depth, because at any reasonable initial scale the high-dimensional centers are already out of range of the data.

### A kernel is a readout, not a representation {#last-layer-not-hidden}

These failures share one root, and it is the same duality this book has celebrated from the start. A kernel is a *fixed* similarity, an inner product in a feature space you do not get to reshape. That is exactly what you want in the *last* layer, the readout, where the representer theorem applies, the problem stays convex, and the fixed geometry is the source of the guarantees in Parts II through IV. A hidden layer has the opposite job: it must *learn a representation*, which means transporting its nonlinearity's sensitive region to wherever the data actually is. A rectifier's sensitive region is a hyperplane whose gradient is constant on the active side, so gradient descent moves it freely; a Gaussian's sensitive region is a small fixed ball whose gradient vanishes the moment the ball is in the wrong place, so gradient descent cannot move it there. The neural tangent kernel view of this chapter says the same thing from the other end: in the wide, lazy regime the features do not move at all, and for a Gaussian layer that immobility is not a limit reached only at infinite width but the operating condition at every width, imposed by the vanishing signal.

::: {.remark}
[The modern reading]{.box-title}

None of this says kernels and deep learning are incompatible; the whole chapter argues the reverse. It says that the naive substitution, a fixed local kernel dropped in where a rectifier goes, asks the kernel to do the one thing a fixed kernel cannot, learn its own geometry. The convolutional kernel networks of this chapter succeed as deep kernel machines precisely because they do not make that substitution: they keep the kernel as a fixed, well-conditioned local embedding and learn a small set of anchor points through the Nyström projection, so the representation is shaped by end-to-end training while the kernel supplies the geometry. The lesson is not that the kernel neuron was a bad idea but that a hidden unit must be able to move its own attention, and a fixed bump cannot. The theoretical statement of this gap, the mean-field limit in which hidden units do move and the larger-than-RKHS function space they explore, is the subject of the next chapter, [[ch:kernels-now|Kernels in the Modern World]].
:::

## Conclusion {#conclusion}

The thread running through this chapter is a single idea seen from many sides: a deep architecture is a map into a reproducing kernel Hilbert space. Fixing the hidden layer sends the infinite-width network to the random-feature kernel; letting gradient descent run on a very wide network sends its trajectory to the neural tangent kernel; and building each layer as a Nyström-approximated kernel embedding places an entire convolutional, graph, or recurrent network inside an RKHS, where it becomes one linear functional of a data-independent feature map \(\Phi\). Once there, the RKHS norm becomes a single lever controlling smoothness, deformation stability, and generalization at once, the pieces we treated separately as the geometry factor and the complexity factor of the basic inequality \(\lvert f(\mathbf{x}) - f(\mathbf{x}') \rvert \leq \lVert f \rVert_{\mathcal{H}} \lVert \Phi(\mathbf{x}) - \Phi(\mathbf{x}') \rVert_{\mathcal{H}}\). Kernels do not compete with deep learning; they supply the geometry within which deep learning can be understood, regularized, and, through the learned anchor points, trained end to end.

::: {.exercises}
## Common mistakes and practical implications {#common-mistakes-and-practical-implications}

Do not collapse NNGP, random-feature, and NTK limits into one statement. The NNGP describes the prior covariance at initialization; a frozen random layer estimates its population kernel; the NTK describes linearized training when the tangent features remain nearly fixed. A finite network inherits none of these conclusions merely by being wide: measure empirical-kernel error, parameter or feature movement, and task-level disagreement.

Depth introduces its own failures. Correlations can collapse toward one, local bandwidths can become useless after rescaling, and a fixed bump used as a hidden neuron can lose its gradient signal. For convolutional kernel networks, report anchor count, Nyström conditioning, pooling scale, deformation assumptions, and the non-kernel architecture used as a baseline.

## Summary and further reading {#summary-and-further-reading}

Width exposes two kernel views of a network: a Gaussian-process covariance at initialization and a tangent kernel for lazy training. Depth turns their scalar kernels into recursions, while Nyström projection turns an infinite local embedding into a trainable finite layer. The common construction, extract local patterns, embed them, pool them, extends from images to graphs and sequences. Its limitation is equally important: a fixed kernel is a stable readout geometry, not automatically a representation that can move toward the task. The Gaussian-process limit begins with [@neal1996], arc-cosine kernels with [@cho2009], and finite random approximations connect back to [@rahimi2007].

## Exercises {#exercises}

1.  [warm-up]{.ex-tag} Schoenberg's theorem says that \(K(\mathbf{x}, \mathbf{y}) = \kappa(\langle \mathbf{x}, \mathbf{y} \rangle)\) is positive definite on the sphere of every Hilbert space exactly when \(\kappa\) has a power series with non-negative coefficients. Decide which of the following univariate functions passes the test on \([-1, 1]\), and for each one that fails, name the offending coefficient: (a) the exponential \(\kappa(u) = e^{\alpha(u - 1)}\) with \(\alpha \gt 0\); (b) the inverse polynomial \(\kappa(u) = 1/(2 - u)\); (c) the polynomial \(\kappa(u) = (c + u)^p\) with \(c \geq 0\) and integer \(p \geq 1\); (d) \(\kappa(u) = 1 - u\); (e) \(\kappa(u) = \cos u\). For (b), expand the geometric series \(1/(2 - u) = \tfrac{1}{2}\sum_{j \geq 0} (u/2)^j\) and read off the sign of every coefficient.
2.  [computation]{.ex-tag} Take two unit vectors \(\mathbf{z}, \mathbf{z}'\) on the sphere separated by angle \(\theta = \pi/3\), so \(\langle \mathbf{z}, \mathbf{z}' \rangle = \tfrac{1}{2}\). (a) Using the table of dot-product kernels, evaluate the degree-1 arc-cosine kernel \(\tfrac{1}{\pi}\big(\sin\theta + (\pi - \theta)\cos\theta\big)\) and Vovk's degree-3 kernel \(\tfrac{1}{3}(1 + u + u^2)\) at this angle, to three decimals. (b) Confirm the identity \(\lVert \mathbf{z} - \mathbf{z}' \rVert^2 = 2 - 2\langle \mathbf{z}, \mathbf{z}' \rangle\) here, and use it to check that the exponential kernel \(e^{\alpha(\langle \mathbf{z}, \mathbf{z}' \rangle - 1)}\) equals the Gaussian \(e^{-\frac{\alpha}{2}\lVert \mathbf{z} - \mathbf{z}' \rVert^2}\) at \(\alpha = 2\). (c) Evaluate the degree-0 angular part \(J_0(\theta) = \pi - \theta\) and confirm it decreases as the angle opens, consistent with its reading as a count of separating hyperplanes.
3.  [proof]{.ex-tag} Derive the degree-0 arc-cosine kernel from a random layer of step neurons. Let \(\sigma\) be the Heaviside step, \(\sigma(t) = 1\) if \(t \gt 0\) and \(0\) otherwise, and draw \(\mathbf{w} \sim \mathcal{N}(0, I)\). Show that

$$ K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w}}\!\left[ \sigma(\mathbf{w}^\top \mathbf{x})\, \sigma(\mathbf{w}^\top \mathbf{y}) \right] = \mathbb{P}\!\left(\mathbf{w}^\top \mathbf{x} \gt 0 \ \text{and}\ \mathbf{w}^\top \mathbf{y} \gt 0 \right) = \frac{\pi - \theta}{2\pi}, $$

    where \(\theta = \arccos\big(\mathbf{x}^\top \mathbf{y} / (\lVert \mathbf{x} \rVert \lVert \mathbf{y} \rVert)\big)\). Conclude that this expectation depends on the inputs through the angle alone, and relate it to the Cho and Saul factor \(J_0(\theta) = \pi - \theta\). Why does the answer not change if you replace \(\mathbf{x}\) by \(\lambda \mathbf{x}\) for any \(\lambda \gt 0\)?
    Hint

    ::: hint-body
    The pair \((\mathbf{w}^\top \mathbf{x}, \mathbf{w}^\top \mathbf{y})\) is jointly Gaussian and centered, so only the plane spanned by \(\mathbf{x}\) and \(\mathbf{y}\) matters; project \(\mathbf{w}\) onto it. The direction of \(\mathbf{w}\) in that plane is uniform on the circle by rotational invariance of \(\mathcal{N}(0, I)\), so the probability that a uniformly random half-plane through the origin puts both \(\mathbf{x}\) and \(\mathbf{y}\) on its positive side is the fraction of directions lying within \(\pi/2\) of both, which is \((\pi - \theta)/(2\pi)\). Positive scaling of \(\mathbf{x}\) leaves the sign of \(\mathbf{w}^\top \mathbf{x}\) untouched.
    :::
4.  [proof]{.ex-tag} Make precise the claim that a wide random layer is a Monte Carlo estimate of \(K_{\mathrm{RF}}\). Fix inputs \(\mathbf{x}, \mathbf{y}\), draw \(\mathbf{w}_1, \ldots, \mathbf{w}_m\) independently from \(\mathcal{N}(0, I)\), and set the empirical kernel

$$ \widehat{K}_m(\mathbf{x}, \mathbf{y}) = \frac{1}{m} \sum_{i=1}^m \sigma(\mathbf{w}_i^\top \mathbf{x})\, \sigma(\mathbf{w}_i^\top \mathbf{y}). $$

    Assume the feature product \(\xi_i = \sigma(\mathbf{w}_i^\top \mathbf{x})\, \sigma(\mathbf{w}_i^\top \mathbf{y})\) has finite variance \(v(\mathbf{x}, \mathbf{y}) \lt \infty\). Show that \(\mathbb{E}\big[\widehat{K}_m(\mathbf{x}, \mathbf{y})\big] = K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y})\) for every \(m\) (the estimate is unbiased), that \(\operatorname{Var}\big[\widehat{K}_m(\mathbf{x}, \mathbf{y})\big] = v(\mathbf{x}, \mathbf{y}) / m\), and hence that \(\widehat{K}_m(\mathbf{x}, \mathbf{y}) \to K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y})\) with a typical fluctuation of order \(1/\sqrt{m}\). Explain in one sentence why this is exactly the statement that widening a frozen hidden layer refines a kernel estimate rather than changing the kernel it targets.
    Hint

    ::: hint-body
    The \(\xi_i\) are independent and identically distributed with common mean \(K_{\mathrm{RF}}(\mathbf{x}, \mathbf{y})\), so linearity of expectation gives the mean and independence gives \(\operatorname{Var}[\frac{1}{m}\sum_i \xi_i] = \frac{1}{m^2}\sum_i \operatorname{Var}[\xi_i] = v/m\). The \(1/\sqrt{m}\) rate is the standard deviation, the square root of the variance; the law of large numbers then forces convergence to the population mean, which is the kernel.
    :::
5.  [proof]{.ex-tag} Derive the two-term neural tangent kernel for the two-layer network \(f_\theta(\mathbf{x}) = \tfrac{1}{\sqrt{m}} \sum_{i=1}^m v_i\, \sigma(\mathbf{w}_i^\top \mathbf{x})\) with parameters \(\theta = (v_i, \mathbf{w}_i)_i\). (a) Compute the two blocks of the gradient, \(\partial f_\theta / \partial v_i\) and \(\nabla_{\mathbf{w}_i} f_\theta\), and check that each carries a factor \(1/\sqrt{m}\). (b) Form the gradient inner product \(\langle \nabla_\theta f_{\theta_0}(\mathbf{x}), \nabla_\theta f_{\theta_0}(\mathbf{y}) \rangle\), summing both blocks over \(i\). (c) With \(\mathbf{w}_i, v_i\) drawn from \(\mathcal{N}(0, I)\) and \(\mathbb{E}[v_i^2] = 1\), take \(m \to \infty\) and apply the law of large numbers to obtain

$$ K_{\mathrm{NTK}}(\mathbf{x}, \mathbf{y}) = \mathbb{E}_{\mathbf{w}}\!\left[ \sigma(\mathbf{w}^\top \mathbf{x})\, \sigma(\mathbf{w}^\top \mathbf{y}) + (\mathbf{x}^\top \mathbf{y})\, \sigma'(\mathbf{w}^\top \mathbf{x})\, \sigma'(\mathbf{w}^\top \mathbf{y}) \right]. $$

    Identify which term comes from the output weights and which from the hidden weights, and explain why freezing the hidden layer deletes the second term and leaves exactly \(K_{\mathrm{RF}}\).
    Hint

    ::: hint-body
    Differentiating gives \(\partial f_\theta / \partial v_i = \tfrac{1}{\sqrt{m}} \sigma(\mathbf{w}_i^\top \mathbf{x})\) and \(\nabla_{\mathbf{w}_i} f_\theta = \tfrac{1}{\sqrt{m}} v_i\, \sigma'(\mathbf{w}_i^\top \mathbf{x})\, \mathbf{x}\). The \(v\)-block sum is \(\tfrac{1}{m}\sum_i \sigma(\mathbf{w}_i^\top \mathbf{x})\sigma(\mathbf{w}_i^\top \mathbf{y})\); the \(\mathbf{w}\)-block sum is \(\tfrac{1}{m}\sum_i v_i^2\, \sigma'(\mathbf{w}_i^\top \mathbf{x})\sigma'(\mathbf{w}_i^\top \mathbf{y})\, (\mathbf{x}^\top \mathbf{y})\). Each empirical average converges to its expectation; use \(\mathbb{E}[v_i^2] = 1\) and independence of \(v_i\) from \(\mathbf{w}_i\).
    :::
6.  [exploration]{.ex-tag} Open the interactive figure above in the section on random feature kernels. It plots a target dot-product kernel against the estimate produced by a finite bank of \(D\) random features, the same object a width-\(D\) hidden layer computes. Start at small \(D\), where the estimate is visibly rough and rattles around the smooth target curve, then raise \(D\) and watch the estimate settle onto its infinite-width limit. Answer three questions from what you see: (a) roughly how does the size of the wiggle around the target shrink as you multiply \(D\) by four, and does that match the \(1/\sqrt{m}\) rate you proved above? (b) the target curve itself never moves as \(D\) grows, only the estimate tightens onto it; which fact about frozen random features does that illustrate, as opposed to what would happen if the layer were being trained? (c) in the language of the chapter, in what precise sense is each additional random feature one more Monte Carlo sample of the population inner product \(\mathbb{E}_{\mathbf{w}}[\sigma(\mathbf{w}^\top \mathbf{x})\sigma(\mathbf{w}^\top \mathbf{y})]\)?
7.  [challenge]{.ex-tag} Audit the lazy-training argument rather than assuming its conclusion. Consider the linearization \(f_\theta(\mathbf{x}) \approx f_{\theta_0}(\mathbf{x}) + \langle \theta - \theta_0,\, \nabla_\theta f_{\theta_0}(\mathbf{x}) \rangle\) for \(f_\theta = \tfrac{1}{\sqrt{m}}\sum_i v_i \sigma(\mathbf{w}_i^\top \mathbf{x})\). (a) Explain why the scaling makes an order-\(1/\sqrt m\) displacement per neuron *compatible* with an order-one output change. (b) Name at least three additional controls needed to conclude that the tangent feature map remains frozen during training. (c) Under those controls and squared-loss gradient flow, diagonalize the fixed tangent Gram matrix and derive \(\mathbf r_t=e^{-t\Theta}\mathbf r_0\). State when interpolation follows and why this is not automatically kernel ridge regression. (d) Give two ways to leave the regime, and identify where hidden-weight derivatives appear in the two-term NTK of the previous exercise.
    Hint

    ::: hint-body
    The \(1/\sqrt m\) sum permits \(m\) coherent order-\(1/\sqrt m\) neuron-level changes to accumulate to an order-one function change, but scaling alone does not bound the realized trajectory. One also needs a fixed parameterization and learning-rate scale, a controlled finite time horizon or integrated training direction, a Jacobian-Lipschitz bound in the visited neighborhood, concentration of the initial tangent Gram matrix, and nondegeneracy if interpolation is claimed. With a fixed symmetric \(\Theta=Q\Lambda Q^\top\), the residual equation \(\dot{\mathbf r}=-\Theta\mathbf r\) gives \(\mathbf r_t=Qe^{-t\Lambda}Q^\top\mathbf r_0\); it converges to zero exactly on positive-eigenvalue directions, and full interpolation requires \(\lambda_{\min}(\Theta)\gt 0\). Finite-time gradient flow is an early-stopping spectral filter, not ridge, and the initial function remains unless centered away. Mean-field scaling, width-dependent learning rates, long uncontrolled training, or substantial feature motion can leave the lazy regime. Hidden-weight derivatives produce the \((\mathbf{x}^\top\mathbf{y})\mathbb E[\sigma'\sigma']\) term.
    :::
8.  [challenge]{.ex-tag} Trace the Nyström bridge that turns an infinite-dimensional kernel into a finite neural-network layer, and follow it onto graphs. (a) For a dot-product kernel \(K(\mathbf{x}, \mathbf{y}) = \kappa(\langle \mathbf{x}, \mathbf{y} \rangle)\) with feature map \(\varphi\) and anchors \(Z = [\mathbf{z}_1, \ldots, \mathbf{z}_p]\), the Nyström embedding is \(\psi(\mathbf{x}) = \kappa(Z^\top Z)^{-1/2}\, \kappa(Z^\top \mathbf{x})\). Reading the three factors from right to left, identify which is a linear map, which is a pointwise nonlinearity, and which is a second linear map, and explain in what sense the factor \(\kappa(Z^\top Z)^{-1/2}\) is what distinguishes projecting onto a subspace of the RKHS from applying an arbitrary nonlinearity as a plain CNN does. (b) Verify the normalization role of \(\kappa(Z^\top Z)^{-1/2}\) by showing that when the anchors are orthonormal in feature space, so \(\kappa(Z^\top Z) = I\), the embedding reduces to \(\psi(\mathbf{x}) = \kappa(Z^\top \mathbf{x})\). (c) A one-layer graph convolutional kernel network replaces patches by paths: it enumerates the paths \(\mathcal{P}_k(G, u)\) from each node \(u\), embeds each path's attributes through the same Nyström layer, and sums. Write the node update \(\psi(u) = \sum_{p \in \mathcal{P}_k(G, u)} \kappa(Z^\top Z)^{-1/2} \kappa(Z^\top \psi_{\text{prev}}(p))\) and say which of the three CKN moves (extract local patterns, embed with a kernel, pool) each piece implements, and why the anchors \(Z\) are now interpretable as learned prototypical path features.
    Hint

    ::: hint-body
    In \(\psi(\mathbf{x}) = \kappa(Z^\top Z)^{-1/2} \kappa(Z^\top \mathbf{x})\), the innermost \(Z^\top \mathbf{x}\) is linear, the entrywise \(\kappa(\cdot)\) is the nonlinearity, and left-multiplication by \(\kappa(Z^\top Z)^{-1/2}\) is the second linear map; a CNN keeps the first two and drops the third, which is precisely the orthogonal projection onto \(\mathcal{F} = \operatorname{span}(\varphi(\mathbf{z}_1), \ldots, \varphi(\mathbf{z}_p))\). For (c), path enumeration is patch extraction, the Nyström layer is the kernel embedding, and the sum over paths is pooling; the anchors sit in path-feature space, so each \(\mathbf{z}_j\) is a template local pattern the layer scores against.
    :::
:::
